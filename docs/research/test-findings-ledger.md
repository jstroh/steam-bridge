# Test Findings Ledger

Last reviewed: 2026-08-12

This is the fast index for deciding whether a live, manual, expensive, negative,
or environment-sensitive experiment should be run again. Detailed artifact
history remains in the linked platform documents.

## Ledger Rules

- Search this file before a live experiment.
- Status values are `SETTLED`, `OPEN`, `ENVIRONMENT BLOCKER`, `CLOSED PATH`,
  `DIAGNOSTIC ONLY`, and `SUPERSEDED`.
- Preserve negative and inconclusive results. Update or supersede an existing
  entry instead of adding the same experiment again.
- Every entry must say what changed before it is worth repeating.
- Routine unit/type/build checks belong in `current-work.md` and CI, not in this
  ledger.
- Live cross-platform qualification owns one Steam client at a time. Force
  close Steam and its web/overlay helpers on every non-target test machine,
  verify zero remnants, and only then start Steam on the target.
- Never record private app/product/account names or IDs, item IDs,
  order/transaction/Steam IDs, keys, prices, checkout URLs, private fixture
  paths, or raw private payloads.
- Electron QA and release candidates must use an exact stable Electron version.
  Alpha, beta, nightly, and every other prerelease are closed paths. When a fix
  exists only upstream, record its exact commit/TODO instead of testing another
  prerelease. Any product-owner release exception must be exact, executable,
  evidence-bound, receipt-visible, and preserve every unaffected gate.

### Active product-owner release exception

`MAC-CHROMIUM-POST-RESTORE-HALF-RATE-001` no longer blocks the current release.
The product owner accepted that one upstream Chromium defect on 2026-07-28
rather than waiting indefinitely for M152. FOV4 schema-v2 final QA permits it
only with explicit `--allow-known-upstream-cadence-defect`, exact stable
Electron 43.2.0, the `display-pacing-transition`/`fps_below_display_rate`
combination, the retained factor-two/skipped-vsync signature, exact focus, and
healthy policy-rate attached-Metal presentation: selected display rate while
active, and the documented at-most-60-FPS discovery cadence while passive.
Every accepted profile is
receipt-visible with the exact upstream commit and evidence path. This
supersedes the older row's wait-for-stable repeat condition; it does not reopen
prerelease Electron, feature flags, synthetic input, child recreation, or
popup/companion paths.

### Active Windows shared-texture qualification

The 2026-08-12 asynchronous D3D11 fence change materially alters the texture
ownership/copy boundary covered by `WIN-ELECTRON-TOPLEVEL-001` and
`WIN-MOVEMENT-CADENCE-001`. Treat their earlier live passes as architectural
history, not qualification of the new copy path. The fresh source-linked actual-
game pass on the available 165 Hz display is green: settled paint, accepted and
completed GPU copies were 165.0 FPS, native presentation was 164.5 FPS, the
mode was `d3d11-fence-async`, the process-wide depth remained bounded at four,
and timeout/device-loss/CPU-upload/bitmap/missing-frame counters were zero.
Resize, maximize/restore, fullscreen/restore, movement, and clean exit passed.
The exact player 75 Hz configuration remains the decisive external retest for
the original report; require the same fields there and no sustained saturation.

## Windows x64

| ID | Status | Finding | Repeat only when | Evidence |
| --- | --- | --- | --- | --- |
| `WIN-KEYBOARD-LAYOUT-001` | `OPEN` | The standalone native host becomes Windows' focused input owner, so leaving its creation thread on the process default layout can silently replace the layout that was active in the foreground Electron window. The source-linked repair inherits that foreground thread HKL before HWND creation and publishes host/foreground layout diagnostics. On the only installed English layout, creation and a browser-to-game focus round trip retained exact equality, and `/` opened the configured consumer's in-game command suggestions. A distinct configured-consumer defect also converted unmapped Win32 numpad/lock virtual keys directly to characters. The repair uses Electron's documented numpad/lock accelerator names, supplies `iskeypad`, rejects unsupported virtual keys, and forwards Steam Bridge's optional Windows `capsLock`/`numLock` toggle metadata while leaving text on the layout-aware `WM_CHAR` path. In the actual game, numpad `8` and multiply reached Chromium as `Numpad8`/`NumpadMultiply` at location 3, Caps Lock produced uppercase `A` with `capslock`, and Num Lock set/cleared `numlock`; both toggles were restored. The reported Hungarian case remains unproven because this machine has no second layout. | A real supported Windows host already has a non-English layout available, the reporter retests the fixed build, or native surface creation/input-language handling changes. Require the active layout to remain selected and layout-dependent chat punctuation to work; do not hard-code layout-dependent text translation in the consumer. | [Windows input/cadence checkpoint](current-work.md#2026-08-02-windows-keyboard-layout-and-movement-cadence-checkpoint), [native standalone host](../../crates/native/src/native_surface.rs) |
| `WIN-MOVEMENT-CADENCE-001` | `OPEN` | Earlier actual-game evidence proved the same architecture can sustain approximately 165 FPS on a 165 Hz Windows display. A later production corpus of 613 records from 45 sessions invalidates the `0.3.23` adaptive repair: 17 of 21 current/current-like sessions ended below full refresh and nine ended at one quarter refresh, including 144 -> 36/48 and 165 -> 41. One 180 Hz sample lowered the target to 60 even though DXGI was still near 180, proving source-rate stalls were misclassified. Because no upward recovery existed, a transient stall became a permanent cap. Separate older evidence retained near-refresh texture imports while presentation collapsed to 4-5 FPS and async wait timeouts reached thousands, proving an unsignaled DXGI wait can chain visible stalls independently of texture transport. The repair disables all automatic downshifts, bounds the async wait to 25 ms, and permanently switches an unresolved waitable object to `Present(0, DXGI_PRESENT_DO_NOT_WAIT)`. Replacement JavaScript sessions synchronize the native bypass state and use a bounded timer with one-millisecond wake compensation. A deterministic source-linked 165 Hz proof held target 165 through 30 settled fallback samples: median paint 164.45 FPS, median native present 160.75 FPS, maximum sampled Present 0.878 ms, maximum pump 163.4 FPS, exactly one timeout, and zero device loss. The first immutable `0.3.31` package proof then exposed timer coalescing specific to the retained-frame fallback: the ordinary Steam Friends overlay presented at only 65 FPS after fallback while gameplay returned to 164-165 FPS. Its receipt failed closed and the package was not published. The fallback now requests a one-millisecond multimedia timer period only for its renderer lifetime and exposes requested/active diagnostics; exact `0.3.32` must repeat the candidate-bound gameplay and overlay gate. | External reporter retests remain required for exact 144/180/200 Hz hardware before settling the broader finding. Repeat locally only if renderer scheduling, D3D import/copy/presentation, display-rate policy, movement input, or the fallback changes. Require no automatic downshift, no repeated async timeout, no blocking Present, and settled gameplay and overlay presentation at least 95% of refresh. | [Production cadence correction](current-work.md#2026-08-11-windows-production-cadence-correction-0325-candidate), [native D3D renderer](../../crates/native/src/windows_d3d11.rs) |
| `WIN-QA-OVERLAY-FOCUS-001` | `SETTLED` | An explicit configured-consumer QA-menu command was invoked after the game was already backgrounded and produced an Electron hang. That is invalid QA ordering, not a product overlay route: the menu command now requires the native game state to be focused and, on Windows, the native-host diagnostics to report the host foreground before dispatch. Focused activation still succeeds; backgrounded activation is refused. Product checkout, subscription, and ordinary shortcut paths are unchanged, and the fresh post-repair process recorded no crash or hang. | The QA menu dispatch, native focus/foreground diagnostics, or focused activation behavior changes or regresses. Do not use a background debugger command to manufacture overlay activation without a valid presentation target. | [Windows input/cadence checkpoint](current-work.md#2026-08-02-windows-keyboard-layout-and-movement-cadence-checkpoint), [configured-consumer gate](../../../fov4-steam/main/main.js) |
| `WIN-CONSUMER-IN-SURFACE-CHOICES-001` | `SETTLED` | The configured client's Bugdesk report type, severity, and category used native HTML select popups, which exist outside Electron's offscreen game surface and were not interactive through the standalone native host. They now render as labelled listbox buttons inside the captured game surface, expose the label plus current value to assistive technology, and retain an open list with valid focus if asynchronous option hydration completes during interaction. The focused actual-game check selected all three, scrolled the long category list, and proved Escape closes the list before the unsent dialog. | The Bugdesk choice implementation, option hydration, offscreen renderer, native-host input forwarding, or keyboard/focus behavior changes or a focused report-dialog case regresses. Do not restore native select/popover UI outside the game surface. | [configured-consumer checkpoint](current-work.md#2026-08-02-configured-consumer-bugdesk-and-external-link-checkpoint), [client overlay](../../../fov4-client-px-steam-commerce/src/app/bugdesk/PxBugdeskOverlay.ts) |
| `WIN-CONSUMER-EXTERNAL-BROWSER-001` | `SETTLED` | The complete production user-action inventory is the TOS YouTube video, HUD Database root, generated Character Open Build URL, and Welcome Bluesky profile. All cross the explicit Steam preload bridge. The configured shell accepts only the three exact HTTPS hosts those four routes need and calls Electron's system-browser API on Windows, macOS, Linux Desktop, and Steam Deck Desktop Mode; Steam Deck Game Mode returns `false` before dispatch and opens nothing. The client awaits that result, so Open Build cannot report false success. The focused Windows actual-game check opened the database in a separate Chrome tab while leaving the native game host intact; generated Build routing, malicious-URL rejection, and Game Mode denial are source/unit proven. Website-only Stripe, internal logout/recovery, and in-game window-manager calls are classified separately and do not cross this route. | The link inventory or allowlist, Steam preload IPC, Game Mode detection, Electron external-browser API, or client link routing changes or regresses. Never open a browser inside the game surface, do not launch any external browser from Game Mode, and re-audit all navigation call sites before adding a host. | [configured-consumer checkpoint](current-work.md#2026-08-02-configured-consumer-bugdesk-and-external-link-checkpoint), [configured-consumer route](../../../fov4-steam/main/steam-commerce-route.js) |
| `WIN-CANDIDATE-IMMUTABILITY-001` | `SETTLED` | Exact signed `3abcc3f` passed one unprotected no-input boundary but mutated on its next preflight; explicit logging likewise survived one disposable pass and mutated on pass two. This rejected log routing and one-pass proof. The canonical NTFS boundary gives the interactive identity read/execute only while retaining SYSTEM/Administrators full control. It denied a root write, remained executable, passed two consecutive eight-comparison disposable runs, retained its ACL/binding through a rename round trip, and passed exact helper CI. The invalid active was preserved, a fresh protected copy was transactionally activated, and its candidate-bound no-input boundary plus independent post-run audit retained the exact 114-file binding, ACL, signatures, rollbacks, cleanup, and Steam continuity. | Continue binding and ACL audits after every receipt profile. Repeat the two-pass disposable stress only if ACL semantics, identity, Electron/Crashpad, or the helper changes. Preserve mutations; never delete, exclude, or baseline them. | [candidate protection](../../scripts/windows-protect-release-candidate.ps1), [candidate fingerprint](../../scripts/windows-release-candidate-fingerprint.cjs), [render-health probe](../../scripts/windows-render-health-probe.ps1), [matrix](../../scripts/windows-overlay-matrix.ps1), [Electron logging switches](https://www.electronjs.org/docs/latest/api/command-line-switches), [Chromium logging source](https://chromium.googlesource.com/chromium/src/+/HEAD/base/logging.h), [replacement Release](https://github.com/jstroh/steam-bridge/actions/runs/29363098329), [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-SESSION-001` | `SETTLED` | SSH Session 0 cannot prove Steam IPC or overlay behavior. It produced no-client/DXGI-unavailable evidence while the same package worked through an interactive Session 1 task. Never classify a bridge regression from a Session 0 live run. | Steam and the app can demonstrably run through a new same-interactive-session mechanism, or Windows/Steam changes its session model. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-HEALTH-001` | `SETTLED` | Fresh Steam CEF/GPU restarts, swap-chain failures, resource exhaustion, or orphan overlay helpers are Steam-client health blockers. More route loops add no evidence while readiness is unhealthy. | Readiness becomes healthy after bounded cleanup or one evidence-backed restart. Stale historical signals alone are not a reason to restart. | [Windows position](native-overlay-presenter-plan.md#windows-position) |
| `WIN-RENDER-001` | `SETTLED` | Plain rendering with in-process GPU unset is the default. Explicit in-process GPU blanked the tested Electron package. DirectComposition-off rendered in comparisons but remains diagnostic because of crash/ghost-window risk. | Electron/Chromium, GPU driver, Steam client, or composition code materially changes, or the default render-health gate regresses. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-D3D11-COLD-HOOK-001` | `SETTLED` | A true cold Windows launch showed that creating and pumping the D3D host once does not prove Steam's `Present` hook is ready: the first managed activation could be lost even after Steam discovered the surface. A fixed five-second fallback passed once and failed on the next cold cycle, so it is rejected. Steam now initializes before Electron graphics-device creation, and a managed readiness wait continuously presents complete frames on the transparent, non-activating, click-through D3D host until `IsOverlayEnabled` positively confirms the hook. The hold releases to zero FPS on ready, abort, timeout, or close, and no activation or client-session checkout operation runs first. The rebuilt source passed five consecutive Steam shutdown/restart Friends cycles with visible input/close/focus return, readiness varying from roughly 1.0 to 4.8 seconds, and zero crash dumps. | Repeat after Steam/Electron startup ordering, persistent-presenter readiness, polling, activation or checkout reservation, D3D host creation/presentation, Steam/Electron versions, or the cold route changes or regresses. Do not replace the positive handshake with a delay or Chromium GPU flags. | [presenter readiness](../../packages/steam-bridge/src/index.ts), [smoke bootstrap](../../examples/electron-basic/main.js), [current checkpoint](current-work.md#current-state), [Valve overlay guide](https://partner.steamgames.com/doc/features/overlay?language=english), [Valve `ISteamUtils`](https://partner.steamgames.com/doc/api/isteamutils?l=english) |
| `WIN-ELECTRON-HOST-REENTRY-001` | `DIAGNOSTIC ONLY` | A same-session, source-linked Electron consumer reproduced a persistent D3D11 host covering the non-client frame, idle flicker, and a hang/crash during title-bar movement. Content-bounds tracking, a presenter-only message pump, terminal geometry synchronization, DWM clipping, and passive parking repaired those geometry/lifecycle failures at 1024x768. Subsequent overlay-active testing showed File/menu input, title dragging, and maximize behavior still unavailable or incorrect even when diagnostics proved the popup was confined to content. Matching NW.js evidence and Steam-compatible input-hook source show that overlay-active application input suppression is distinct from popup hit testing. This path is not release-ready. | Repeat only after the authoritative native-host prototype or the attached-presenter input model materially changes. Do not retry DevTools-driven or purchase-confirmation interaction. | [native host](../../crates/native/src/native_surface.rs), [presenter lifecycle](../../packages/steam-bridge/src/index.ts), [architecture research](electron-steam-overlay-architecture.md), [current checkpoint](current-work.md#current-state) |
| `WIN-ELECTRON-ATTACHED-POPUP-001` | `CLOSED PATH` | Every committed Windows attached presenter was top-level: initial `popup-layered`, the unparented overlapped "control" comparison, or `owned-popup`. Repeated fixes improved individual symptoms but did not change that architecture. Live consumer use reproduced non-client coverage, purple startup/Alt+Tab surfaces, tiny or partial Steam surfaces, DPI seams, lost rounded corners, toolbar/menu/title-drag and maximize conflicts, minimize/focus desynchronization, active move/resize flicker, retained or stale frames, hangs, and crashes. Region clipping, terminal geometry updates, and retained-frame resize stretching cannot make a second top-level HWND inherit parent-client geometry. The current Windows native module physically contains no popup style, owner/parent handle, parent subclass, popup clipping, external-bounds-following, or fallback branch; ABI-compatible native attach calls fail without replacing an existing standalone surface. The JavaScript raw/session/presenter/default-managed attachment entrypoints now also reject before claiming a surface lease or calling native code, and the unreachable Windows deferred-attach branch plus popup-era tests are removed. A regression proves the rejection does not block a subsequent standalone session. A post-pruning actual-game regression passed ordinary Steam Friends, move/size state changes, fullscreen, and clean shutdown without flicker or a crash. | Reopen only if the product explicitly abandons attached mode and chooses the already separate standalone native game host. Never use an owned or unowned popup as an attached fallback, including when child rendering fails. | [architecture research](electron-steam-overlay-architecture.md), [native host history](../../crates/native/src/native_surface.rs), [current checkpoint](current-work.md#operational-notes) |
| `WIN-ELECTRON-CHILD-001` | `CLOSED PATH` | A source-linked, uncommitted Electron experiment replaced the attached owned popup with a real Win32 `WS_CHILD` host. Native geometry, clipping, focus, and minimize behavior became automatic, but Steam reported activation without rendering its overlay into the child swapchain, so that experiment was removed before commit. No historical committed native-host revision contains `WS_CHILD` or `SetParent`; commit `2a24089` records the experiment while committing the separate, successful standalone top-level game-host design. The 2026-07-21 partial child re-entry was stopped after the history audit and before a complete implementation. The no-pixels result is not permission to fall back to a popup. | Reopen only if Steam hook selection, D3D child-swapchain support, or the renderer architecture materially changes. Require visible Steam pixels before broader transition QA. Otherwise use the standalone native game host and make attached Windows mode fail clearly. | [native host](../../crates/native/src/native_surface.rs), [presenter lifecycle](../../packages/steam-bridge/src/index.ts), [architecture research](electron-steam-overlay-architecture.md), [post-compaction checkpoint](current-work.md#read-first-after-compaction-windows-architecture) |
| `WIN-ELECTRON-TOPLEVEL-001` | `SETTLED` | A true top-level Win32 D3D11 host initialized in the Electron main process renders Steam reliably while retaining ordinary native non-client behavior. The production path imports Electron offscreen shared textures on the matching adapter, copies each pooled texture before release, crops an explicit presentation region when provided, aspect-fits that region, and presents through a two-buffer flip-sequential swap chain. A 2026-07-18 source-linked high-refresh run added `DXGI_SWAP_CHAIN_FLAG_FRAME_LATENCY_WAITABLE_OBJECT`, maximum frame latency one, and immediate pumping for new CPU/shared frames while retaining `Present(1)` and the scheduled retained-frame/overlay fallback. On a native 1920 by 1200 display measured by DWM at 164.766 Hz, the original continuous-present diagnostic produced 145-149 game-surface FPS and 162-165 native presents. A later A/B found that disabling continuous presentation froze after import because a copied shared texture was incorrectly cleared as clean; marking each successful import dirty repaired the one-shot contract. With `continuousPresent: false`, ordinary animated gameplay then drove both source and native presentation at roughly 159-165 FPS. A real checkout automatically enabled overlay-driven continuous presentation and held roughly 140-146 source FPS and 155-162 native presents. All phases had zero frame-latency wait timeouts and zero slow shared-texture copies. The same run proved a real accessible Win32 File/Edit/View menu, exact 1280 by 720 logical client geometry with a 640 by 480 logical minimum, title drag, edge resize, rounded restored corners, maximize/minimize/focus return, aspect-preserving fullscreen, menu removal/restoration across fullscreen, and exact windowed placement restoration. A fresh 2026-07-19 pass repeated those window-state and menu paths, exact minimum sizing, default and resized checkout/subscription geometry, focus return, and centered confirmation-dialog adoption without authorizing a transaction. The 2026-08-10 adaptive change keeps `Present(1)` as the default but permits coordinated `Present(2..4)` exact-divisor fallback on sustained high-refresh overload; the locally available 60 Hz actual-game path remained at `Present(1)`. The opt-in animation is diagnostic-only and is not injected into production content; static production content may naturally paint less often. A synthetic Shift+Tab attempt was not accepted by Steam's global hook, so that attempt does not replace physical-hotkey or candidate-bound release proof. | Repeat after native host creation, D3D presentation or frame-latency pacing, adapter selection, texture ownership/copy, presentation crop, menu or minimum-size handling, aspect-fit/input transforms, Electron offscreen capture, Steam/Electron/GPU versions, cursor or shortcut routing, window-state handling, or shared-texture composition materially changes, or a focused case regresses. Do not rerun the rejected child or attached-popup paths for confidence. | [native host](../../crates/native/src/native_surface.rs), [D3D renderer](../../crates/native/src/windows_d3d11.rs), [session API](../../packages/steam-bridge/src/index.ts), [architecture research](electron-steam-overlay-architecture.md), [current checkpoint](current-work.md#current-state) |
| `WIN-OVERLAY-MODAL-CAPTURE-001` | `SETTLED` | A 2026-07-26 source-linked actual-game checkout probe measured `GetCapture()` as the exact standalone D3D host HWND while Steam was active. Title-bar, right-edge, maximize/minimize, system-menu, and fullscreen attempts left host bounds unchanged and produced no `WM_NCLBUTTONDOWN/UP`, `WM_SYSCOMMAND`, or `WM_ENTERSIZEMOVE/WM_EXITSIZEMOVE`. The overlay stayed aligned, stable, focused, and near 165 Hz with no wait timeout, device loss, or slow copy. Escape closed Steam, capture cleared, and title dragging immediately moved the same host. Active window management is therefore a Steam-owned modal constraint; active focus and externally forced display/refresh/resolution/DPI changes remain valid stress. | Repeat only if Steam's capture/input model, host HWND ownership, active overlay lifecycle, or affected Windows input diagnostics materially change, or capture is no longer the exact host or post-close recovery regresses. Never call `ReleaseCapture`, synthesize `WM_NCLBUTTONDOWN`/`DefWindowProc` move loops, or retry popup/`WS_CHILD` hosts to bypass Steam. | [Windows active-modal finding](current-work.md#2026-07-26-windows-steam-modal-capture-finding), [Windows QA contract](windows-actual-game-exhaustive-qa.md#steam-modal-capture-contract) |
| `WIN-STANDALONE-RESIZE-DEVICE-LOSS-001` | `SETTLED` | A long modal keyboard resize of the standalone host reproduced `DXGI_ERROR_DEVICE_REMOVED` from `ResizeBuffers`: the old one-millisecond sizing timer and every `WM_SIZE` both rendered inside the nested move/size loop, repeatedly racing resize and flip work until the visible host failed closed. Modal presentation is now coalesced onto one 16 ms timer, exit performs one terminal render, DXGI device-loss HRESULTs are classified without destroying the host, and the next shared texture rebuilds the renderer on its adapter. Diagnostics expose current loss state and loss/recovery counters. The exact stress was repeated through the enforced 640 by 480 logical minimum at 225% scale, recovered to 59.9 FPS, and retained zero device losses, slow texture copies, crashes, or Steam-client damage. | Repeat after modal move/size scheduling, swap-chain resize/present, device-loss classification/recovery, adapter switching, window minimum sizing, or shared-texture import changes, or a focused resize regresses. Keep rendering coalesced during `WM_ENTERSIZEMOVE`; do not restore per-message flips or a one-millisecond modal timer. | [native host](../../crates/native/src/native_surface.rs), [D3D renderer](../../crates/native/src/windows_d3d11.rs), [current checkpoint](current-work.md#current-state) |
| `WIN-STANDALONE-MINIMIZE-ICONIC-001` | `SETTLED` | Windows reports iconic geometry around `-32000` and a synthetic zero-sized client while a standalone host is minimized. The old path converted that into an approximately 68 by 68 shared texture and continued presenting near 60 FPS. The host now reports `minimized` on `windowChanged` and diagnostics, skips D3D rendering while iconic, and the FOV4 consumer retains the last real viewport while throttling its hidden renderer/session to 1 FPS. On restore it invalidates display-rate caches, resumes the current display target, and reconciles the real client geometry. Actual-game minimize held the full 2883 by 1623 coded source, performed no storage recreation, presented zero FPS, then restored cleanly to 59.9 FPS with zero device loss. | Repeat after iconic/minimize detection, input-event parsing, hidden-renderer pacing, viewport reconciliation, display-rate selection, or native present suppression changes, or a minimize/restore regression. Never resize a renderer from iconic sentinel geometry. | [native host](../../crates/native/src/native_surface.rs), [session API](../../packages/steam-bridge/src/index.ts), [consumer integration](../../../fov4-steam/main/main.js), [current checkpoint](current-work.md#current-state) |
| `WIN-OFFSCREEN-DPI-001` | `SETTLED` | On a Windows desktop at 225% scale, Electron 42/43 with the default offscreen scale factor of 1 continued issuing paint callbacks after live WebGL resize but every CPU and shared-texture capture was black; the same content passed on Electron 41 at native scale and Electron 42/43 with an explicit native display scale. Chromium also allocated the coded texture one logical pixel wider and taller than the game viewport. The consumer now fixes the hidden renderer to its launch display scale, while Steam Bridge's generic `presentationRect` crops the exact application viewport before aspect-fit presentation. The actual Electron 43.1.1 game then remained correctly aligned and live through 1280-by-720 windowed, 1536-by-964 fullscreen, and restored 1280-by-720 transitions at 59-60 FPS against 60 Hz, with zero bitmap fallbacks, frame-latency wait timeouts, or slow shared-texture copies. | Repeat after Electron/Chromium offscreen scale behavior, WebGL capture, presentation-region validation/copy, monitor-DPI policy, shared-texture dimensions, or the actual consumer changes. Do not force a browser-client aspect ratio onto the native game viewport. | [current checkpoint](current-work.md#current-state), [D3D renderer](../../crates/native/src/windows_d3d11.rs), [session API](../../packages/steam-bridge/src/index.ts) |
| `WIN-ELECTRON-STEAM-DIALOG-001` | `SETTLED` | A registry-backed standalone-host checkout cancellation exposed a separate visible foreground `Steam Dialog`/`SDL_app` top-level HWND from `steamwebhelper.exe` with no owner while the D3D11 host stayed dimmed. The repair snapshots exact matching windows at overlay activation and adopts only a newly appearing unowned match as a game-host-owned popup, with process-image verification, centered/following geometry, teardown restoration, and diagnostics. The optimized source-linked Electron run and the final exact registry `0.2.10` run both placed the live dialog above the game, exposed the expected Cancel/Continue controls, removed it cleanly through Cancel Transaction, returned focus to gameplay, and authorized no purchase or subscription. The registry run also passed ordinary overlay open/close, one-time checkout cancellation, title drag, keyboard resize, minimize/restore, maximize/restore, fullscreen, Alt+Tab/focus return, aspect-fit rendering, rounded restored corners, cursor suppression, post-checkout stress, packaged-runtime byte checks, and clean Electron exit code zero without flicker, purple startup pixels, a tiny Steam surface, hang, or crash. | Repeat after dialog identity/ownership/geometry matching, overlay lifecycle signaling, standalone-host creation/teardown, Steam client dialog behavior, or consumer checkout routing materially changes, or a focused case regresses. Do not finalize a purchase or subscription, broaden the matcher, or restore the rejected cross-process `WS_CHILD` path. | [native host](../../crates/native/src/native_surface.rs), [session lifecycle](../../packages/steam-bridge/src/index.ts), [v0.2.10 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.10), [current checkpoint](current-work.md#current-state) |
| `WIN-NATIVE-HOST-DPI-SIZE-001` | `SETTLED` | Published `v0.2.14` retains the `v0.2.11` logical initial-size repair and adds PMv2 thread scoping, logical minimum-client tracking, DPI diagnostics, menu-aware non-client adjustment, an accessible menu scale floor, and stable normal-client restoration across `WM_DPICHANGED`. Exact-candidate proof and the registry consumer retained a 1280 by 720 logical client, 640 by 480 logical minimum, 100%-125%-100% DPI transitions, title drag, edge resize, rounded corners, maximize/restore, minimize/focus return, aspect-fit fullscreen, real menu input/accessibility, and modal checkout open/cancel. | Repeat after logical-size/DPI conversion, menu/non-client adjustment, minimum tracking, work-area placement, `WM_DPICHANGED`, standalone window-state handling, Electron scale policy, Steam modal web routing, or the exact candidate changes. Never finalize a transaction during proof. | [native host](../../crates/native/src/native_surface.rs), [consumer evidence](current-work.md#consumer-evidence), [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-APPCONTROL-001` | `ENVIRONMENT BLOCKER` | Local Authenticode `Valid` is insufficient while verified/reputable App Control policy is enforced; executable, addon, and native dependency loads were blocked. Disabling enforcement on the disposable rig allowed current-package D3D11 proof. | Policy state, signing identity/reputation, or the exact package changes. Always use preflight/native-load evidence; do not retry Steam routes through a known block. | [Windows position](native-overlay-presenter-plan.md#windows-position) |
| `WIN-NATIVE-API-SHAPE-001` | `SETTLED` | The canonical `NativeBinding` declares 1,129 required methods with no missing, unexpected, or duplicate method. Exact `v0.2.14` CI, Release, package gate, executable probe, deployed-candidate binding, four-profile receipt, and published npm tarball retain that count and declaration SHA-256 `7cce9c4c750e754c8b804e6cb7d9ba6a0526d0e03cd16adb8c3f78c5a3c23bf8`. This proves registration and packaging shape, not every account-, app-, server-, transaction-, or state-dependent semantic. | Repeat after a `NativeBinding`, N-API export/macro, native build, Electron/electron-builder/ASAR, candidate-integrity, or receipt-binding change, or when the audit/probe regresses. Do not mass-invoke the API surface. | [Windows package gate](../../scripts/windows-electron-builder-asar-gate.cjs), [API audit](../../scripts/audit-steam-api-coverage.cjs), [v0.2.14 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.14), [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-PACKAGE-001` | `SETTLED` | Exact protected `v0.1.4` at `2f797aa` is the published release source. Its 114-file / 398,180,793-byte Windows candidate retains content SHA-256 `0299d32649b5618149df4d96442989f1c6c5b66351a4c9f7072707601980a570`, binding SHA-256 `ec365fb2feefba92b62eaa1fe4cdb00f485ef2d548dcdd6288dc07ddc46c874c`, canonical ACL, shortcut, rollback preservation, and zero current-run processes/tasks/stages. Ordered persistent-reuse, checkout, shortcut-routes, and managed-routes roots pass 31/31 clean cases and 27 active routes against one unchanged Steam identity. Receipt semantic SHA-256 `6d2302ee100fde1f7123e4454994e18b079599a519d412623f6d64a1e7d91740` binds all four roots to that candidate. The npm tarball is byte-identical to the audited Release artifact and has verified registry signature and provenance. | Repeat only after package contents, binding, ACL, release contract, receipt schema, Steam identity, attached presenter backend, or publication workflow changes, or an audit regresses. Preserve invalid rollbacks and do not rerun live profiles merely for confidence. | [candidate protection](../../scripts/windows-protect-release-candidate.ps1), [v0.1.4 Release](https://github.com/jstroh/steam-bridge/actions/runs/29488457815), [publish run](https://github.com/jstroh/steam-bridge/actions/runs/29491067145), [package gate](../../scripts/windows-electron-builder-asar-gate.cjs), [receipt generator](../../scripts/windows-live-proof-receipt.cjs), [release policy](../../CONTRIBUTING.md#release-candidates-publication-and-rollback) |
| `WIN-DOCS-RELEASE-001` | `SETTLED` | Protected `v0.1.5` passed its build/package gate but correctly failed documentation-only equivalence before npm publication because a rebuilt macOS addon differed from proved `v0.1.4`; preserve it as rejected evidence. Replacement `v0.1.6` restores the nine exact proved native/runtime files before packaging, passes the complete 38-file comparison, and publishes tokenlessly with only `README.md` and package `version` changed. The registry tarball is byte-identical to the retained Release artifact, `latest` is `0.1.6`, signature and provenance verification pass, and the packaged README is the concise 217-line version. This reuses exact runtime proof and does not claim a new live Steam run. | Repeat only after the documentation-only payload restore/equivalence contract changes, a future candidate claims proof reuse, or registry/Release integrity regresses. Never publish or retag `v0.1.5`, and do not rerun unchanged live Steam profiles. | [v0.1.6 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.1.6), [equivalence gate](https://github.com/jstroh/steam-bridge/actions/runs/29536631627), [tokenless publication](https://github.com/jstroh/steam-bridge/actions/runs/29537214839), [publisher verifier](../../scripts/publish-release-candidate.cjs), [release policy](../../CONTRIBUTING.md#release-candidates-publication-and-rollback) |
| `WIN-NATIVE-HOST-RELEASE-001` | `SETTLED` | Exact published `v0.2.14` at `3d0678b` is the supported Windows native-host candidate. Its protected 114-file bundle retains content SHA-256 `de6d5e5166d9f1b2efe23ae471815d2c0ce1b2a2d43cea9acef9ca69a859ef12`, binding SHA-256 `b80aa46d8ea63d70d48bb13fa9fe5ccfe5af5d374a294d627697d6c40876dbe8`, canonical protection, and one Steam identity through all four required profiles. The receipt binds 31/31 clean cases and 27 active routes with D3D11 agreement, high-refresh and DPI geometry, fail-closed modal targeting, input/focus/park lifecycle, passive achievements, cleanup, and zero crashes. The npm tarball is byte-identical to the audited Release artifact, carries SLSA provenance, and is consumed by the final non-junction Electron pass. All five public Release asset digests match the retained local records. | Repeat only after native host, shared-texture/presentation, sizing/DPI, window state, cursor/input, managed presenter, readiness classification, checkout-dialog ownership, proof schema, exact candidate, Electron/Steam, or publication workflow changes, or a later audit regresses. Never move or reuse `v0.2.14` or splice failed roots. | [v0.2.14 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.14), [Release assembly](https://github.com/jstroh/steam-bridge/actions/runs/29681862973), [trusted npm publish](https://github.com/jstroh/steam-bridge/actions/runs/29682898297), [current checkpoint](current-work.md#exact-release-evidence), [release policy](../../CONTRIBUTING.md#release-candidates-publication-and-rollback) |
| `WIN-PACKAGE-SMOKE-HOST-001` | `ENVIRONMENT BLOCKER` | Repository `package:smoke` cannot complete under native Windows semantics: the packaged macOS preparation self-test deliberately expects `/tmp/...` after `path.resolve`, which becomes `C:\\tmp\\...` on Windows. Adding the installed Git Bash and bundled Python only moves the boundary to Linux fixture semantics; installing WSL is not justified for this release slice. Applicable Windows receipt, summary, package, API, unit, and native checks pass locally; exact CI `29389571866` passes the complete Linux package smoke and all three platform jobs. | Repeat locally only if the macOS/Linux fixture suite becomes host-neutral or a supported Unix environment is available. Do not install or reconfigure WSL merely to duplicate exact Linux CI. | [package smoke](../../scripts/smoke-package.cjs), [exact CI](https://github.com/jstroh/steam-bridge/actions/runs/29389571866), [macOS preparation helper](../../packages/steam-bridge/bin/prepare-macos-app.cjs), [contribution checks](../../CONTRIBUTING.md) |
| `WIN-CANDIDATE-BINDING-001` | `SETTLED` | The exact `1028ef2` task exposed Electron ASAR virtualization in the physical candidate fingerprint runner and stopped before preflight or any overlay case. Commit `a6a5553` sets `process.noAsar` in that helper. Replacement Release `29179993719` passed all three prebuilds, Electron `43.1.0` packaging/native load, 114-file archive/audit, and 1,121-method gates; its independently verified tarball then passed the installed-certificate signed gate and transactional deployment with rollback and Steam continuity. The candidate-bound interactive task passed the packaged fingerprint boundary and continued through preflight, native load, render health, and its one presenter-ready case. | Do not rerun `1028ef2` or repeat the `a6a5553` binding probe. Repeat only after the fingerprint helper, Electron, ASAR configuration, JavaScript-runner fallback, candidate archive, or deployed-directory contract changes. | [candidate fingerprint helper](../../scripts/windows-release-candidate-fingerprint.cjs), [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [replacement candidate](https://github.com/jstroh/steam-bridge/actions/runs/29179993719) |
| `WIN-NATIVE-LOAD-AUDIT-001` | `SETTLED` | The exact signed and deployed `a6a5553` no-click task passed candidate binding, readiness, render health, lazy D3D11 selection, presenter-ready, cleanup, and Steam continuity, but its bundled summary falsely demanded attached renderer diagnostics from the deliberately non-attaching native-load gate. Commit `fe7d989` labels lazy selection-only evidence without claiming renderer proof and preserves three-way agreement for attached cases. Its exact CI and replacement Release `29180717107` passed; the independently verified 114-file Electron `43.1.0` candidate then passed the installed-certificate signed gate, transactional deployment with rollback and Steam continuity, and the complete candidate-bound no-click task including its bundled semantic summary. | Do not rerun `a6a5553` or repeat the unchanged `fe7d989` no-click gate. Repeat only when lazy presenter readiness, native-load policy, backend diagnostics, summary semantics, Electron packaging, or attached renderer agreement changes, or a later candidate regresses. | [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [Windows smoke verifier](../../scripts/windows-electron-smoke.ps1), [final candidate](https://github.com/jstroh/steam-bridge/actions/runs/29180717107) |
| `WIN-RECEIPT-AUDIT-001` | `SETTLED` | Native-load and route `10-presenter-ready` require selection-only D3D11 with empty host/renderer/style and zero attached lifecycle; every attaching route requires complete presenter/native-host/renderer D3D11 agreement. Current native-presenter cases require exact `overlayInjection=false`, while Steam launch markers, overlay-enabled state where applicable, activation callbacks, and lifecycle proof remain mandatory. Foreground-grant, close-glyph, and current-panel fallback fields are strictly validated. The exact `v0.2.14` 31-case receipt and independent readback pass and are retained with the public Release. | Repeat only when presenter-ready behavior, backend summaries, injection diagnostics, foreground-grant/close-glyph/current-panel schemas, profile contracts, or receipt generation changes, or a semantically green root is rejected. | [receipt generator](../../scripts/windows-live-proof-receipt.cjs), [matrix summarizer](../../scripts/summarize-windows-overlay-matrix.cjs), [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-RECEIPT-HOST-STYLE-002` | `SETTLED` | Published `v0.2.8` corrected immutable receipt expectations for the Electron-owned `owned-popup` presenter and passive achievement evidence; `v0.2.9`, `v0.2.10`, and `v0.2.11` repeated the complete four-profile proof for their runtime changes. Exact published `v0.2.14` repeats all four profiles for high-refresh, menu/minimum/DPI, transient readiness, and Friends-classifier changes and produces receipt semantic SHA-256 `3daadf8eca5ca79efc71b000c7b69198ee7917498b8d8ca5d1f514676d6d4509`. | Repeat only if presenter host-style diagnostics, passive overlay semantics, receipt validation, checkout-dialog lifecycle, sizing/readiness behavior, or the exact candidate changes, or a later receipt regresses. | [receipt generator](../../scripts/windows-live-proof-receipt.cjs), [v0.2.14 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.14), [current checkpoint](current-work.md#exact-release-evidence), [release policy](../../CONTRIBUTING.md#release-candidates-publication-and-rollback) |
| `WIN-SHORTCUT-001` | `SETTLED` | A read-only audit before the `afc19eb` focused run found no reusable direct packaged-app Steam shortcut: the default public Smoke entry targeted a probe executable without the stable smoke-env-file option, while the remaining entries were historical Electron-runtime, renamed-runtime, or command-launcher diagnostics. The exact signed package was placed at a dedicated stable direct-app path; Steam was closed once; the packaged shortcut suite updated the default entry with a retained backup; and immediate `-AssumeShortcutConfigured` passes returned unchanged/existing-match. After the cleanup fix passed exact CI and signed deployment, Steam was started once in interactive Session 1 with a new continuity identity; the exact `02bb14d` active and shortcut audits and focused case's assumed-shortcut gate all preserved it. After the later exact `8323cd4` case-40 launch, the whole `shortcuts.vdf` hash differed from its captured pre-run hash, while a post-run packaged dry-run still returned unchanged/existing-match for the stable target entry, including its executable, start directory, launch options, game ID, and overlay setting. Whole-file hash drift alone therefore does not establish target-entry drift; the semantic dry-run remains the product gate. | Shortcut name, executable/start path, env-file launch option, package deployment path, stable game ID, overlay setting, helper comparison contract, or targeted semantic dry-run result changes. Do not reuse the historical diagnostic entries, treat whole-file hash drift alone as target drift, or restart Steam solely for shortcut state. | [Current work](current-work.md#exact-next-step), [Windows matrix](../../scripts/windows-overlay-matrix.ps1) |
| `WIN-TASK-CLEANUP-001` | `SETTLED` | Commit `35690a4` remains valid for the earlier `schtasks` exit-capture defect. The signed `414d914` run then exposed numeric-parent-only PID-reuse adoption and strongly implicated wrapper cleanup in stopping Steam, though the missing killed-identity list prevented absolute attribution. Commits `61e688c` and `02bb14d` now require chronological CIM/native creation identity for ancestry and exact same-handle creation match, termination, and wait; recursive `taskkill /T` and PID-only stops are absent. Native PowerShell wrong/exact/gone identity, unrelated-sentinel, stale-ancestry, invalid-root, counter-shape, parser, full repository, exact CI, signing, deployment, and four-generation rollback gates passed. In the one signed `02bb14d` focused run, the wrapper captured 32 tree processes, required zero root/fallback stops after completion, produced two empty verification scans, passed task deletion/package/file/environment guards, and preserved the exact Steam PID/session/start identity through all wrapper cleanup. Matrix package cleanup also ended empty while fail-closing raced or stale identities, without touching Steam. | Cleanup ancestry/identity/runtime, task invocation/deletion, package-process cleanup, launch-environment restoration, process outcome accounting, or wrapper ownership changes; or a future run loses exact Steam continuity or reports an unaccounted stop. Do not repeat a standalone cleanup experiment or rerun `414d914`. | [Current work](current-work.md#exact-next-step), [task wrapper](../../scripts/windows-overlay-task.ps1), [Microsoft `Win32_Process`](https://learn.microsoft.com/en-us/windows/win32/cimwin32prov/win32-process) |
| `WIN-D3D11-001` | `SETTLED` | Explicit D3D11 runs retain broad passing managed evidence. A fresh CI-built and signed addon from `da632f8` also passed an attached public managed-web launch with the backend unset/defaulted: the top-level presenter, native-host diagnostics, and renderer diagnostics all reported `windows-d3d11`. This settles actual default renderer identity; repeatable exact-host automation is separately settled under `WIN-FOCUS-001`, while the fresh Electron `43.1.0` broad-suite rerun remains open. | Renderer selection, native-host/renderer diagnostics, Windows addon build inputs, or the D3D11 presenter materially change, or an attached case shows disagreement among the three required fields. | [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-DPI-001` | `SETTLED` | Physical per-monitor-DPI coordinates remain authoritative. Protected `v0.1.0` deterministically missed the smaller 100%-scale close glyph after eight ordered shortcut routes, while protected `v0.1.1` scales the same 16 diagonal samples from logical distances. Retained 100% and 225% screenshots pass the sampler, and a fresh exact-candidate shortcut root completes all ten routes in order, including the formerly failing 100%-scale user route, with glyph-bound input, close/inactive/park callbacks, focus return, three-way D3D11 agreement, clean crashes, cleanup, unchanged candidate binding, and Steam continuity. | Repeat only if DPI conversion, panel/glyph detection, physical screenshot/input coordinates, close evidence, presenter bounds, or Steam web chrome materially changes, or a later ordered root regresses. Do not use fixed points, hide the pointer, add candidate input, or rerun either rejected `v0.1.0` root. | [Current work](current-work.md#current-state), [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-FOCUS-001` | `SETTLED` | Forced z-order, candidate-directed injected input, timing watchers, chat-coordinated clicks, and `UIAccess` remain rejected. The accepted replacement is a separate local broker window: one deliberate broker-button activation calls documented `AllowSetForegroundWindow(ASFW_ANY)`, then the matrix challenge-binds the fresh acknowledgement to the exact broker binary/process/session/window and launches the candidate without sending it input. Ordered exact `v0.1.4` persistent, checkout, shortcut, and managed roots pass 31/31 cases with no human clicks and one unchanged Steam identity. | Repeat only if Windows foreground policy, broker protocol/binary, grant audit, launch ownership, task/session topology, or the candidate input guard changes, or a later root regresses. Do not resume title-bar/chat loops, candidate `SendInput`, focus APIs, longer waits, or OEM-helper manipulation. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence), [foreground broker](../../scripts/windows-foreground-grant-broker/SteamBridgeForegroundGrantBroker.cs), [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [Microsoft `AllowSetForegroundWindow`](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-allowsetforegroundwindow) |
| `WIN-CLEAN-ENV-001` | `SUPERSEDED` | The environment audit correctly rejected unsupported local VM credentials/GPU paths and unsafe OEM-helper manipulation, but its conclusion that another host was required is superseded by the challenge-bound foreground broker. The current host completed all four ordered receipt profiles without copying Steam state, installing a VM, stopping the OEM component, restarting Steam, or requiring user clicks. | Repeat the environment search only if the broker path becomes unavailable or Windows foreground policy materially changes. The rejected VM/credential/OEM-helper workarounds remain prohibited. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence), [foreground broker](../../scripts/windows-foreground-grant-broker/SteamBridgeForegroundGrantBroker.cs) |
| `WIN-DUPLICATE-OPEN-001` | `SETTLED` | Commit `f46e8ff`, exact CI run `29142166095`, the exact 128-file package, signing, transactional deployment, active-package audit, unchanged native trio, 12 valid signables, rollback retention, and Steam continuity pass. Three earlier launches proved synthetic macOS clicks do not establish the required Parsec/Windows foreground event and correctly remained zero-input. The later focused public run launched already foreground, took the mutually exclusive schema-3 not-required branch with no marker, acknowledgment, or controller click, and passed the semantic audit. It proved exactly one duplicate guard event; `opening` status for generic, named, shortcut, and checkout paths; every direct/wait, shortcut/controller, and checkout attempt suppressed; no checkout operation invocation; one overlay activation; close, inactive, park, stable completion, exact focus return, three-way `windows-d3d11`, zero crashes, task/process/environment cleanup, and preserved Steam continuity. | Gate mapping, suppression helpers, checkout-operation ordering, schema-3 foreground state, presenter lifecycle, or Windows packaging materially changes, or a future parity run fails. Do not repeat the synthetic-click attempts or force the nonforeground branch. | [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence), [smoke action](../../examples/electron-basic/main.js), [Windows matrix](../../scripts/windows-overlay-matrix.ps1) |
| `WIN-PASSIVE-POLL-001` | `SETTLED` | Protected `v0.1.4` requires the Windows false-to-true `needs-present` transition inside the existing bounded wait. Its fresh focused progress case passes reset/store, public `1/2` progress, both callbacks, that transition, passive D3D11 attach/parking, crash, cleanup, and Steam-continuity assertions. The fresh 16-case managed receipt root independently retains the passive progress path and all profile requirements; both are bound to the exact published candidate. | Repeat only if passive priming/polling, notification completion, achievement preparation, presenter attachment/parking, receipt semantics, or the exact candidate changes, or a later focused/managed audit regresses. Do not add timing delays or manually prepare account state. | [Current work](current-work.md#current-state), [smoke action](../../examples/electron-basic/main.js), [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs) |
| `WIN-PERSISTENT-REUSE-001` | `SETTLED` | Protected `v0.1.4` contains the repaired packaged auditor and a fresh persistent root completes all three shown/closed/parked cycles, six ordered activation callbacks, one stable D3D11 attachment, exact focus return, authenticated quit, clean crashes, cleanup, candidate binding/ACL, and Steam continuity. Cycle one retains direct close-glyph proof. Later cycles accept only direct glyph proof or a point reconstructed from that cycle's independently audited current panel geometry, retaining exact host, per-monitor-DPI scale, scale-aware insets, target/pointer equality, distinct screenshots, pre-dispatch focus, and causal close/park checks. The other three fresh receipt profiles pass and the exact receipt is attached to the public Release and npm publication. | Repeat only if persistent lifecycle, host/DPI/inset/glyph geometry, foreground grant, input audit, D3D11 agreement, receipt semantics, or the exact candidate changes, or a later audit regresses. Do not rerun `v0.1.3` or splice historical roots. | [Current work](current-work.md#current-state), [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [v0.1.4 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.1.4) |
| `WIN-PERSISTENT-READINESS-002` | `SETTLED` | Exact published `v0.2.11` persistent reuse passed all three shown/closed/parked cycles against one unchanged candidate and Steam identity. Cycle one retained direct close-glyph proof; later cycles revalidated the current Steam panel, native HWND, physical host rectangle, per-monitor-DPI scale, target containment, foreground ownership, and fresh screenshots before input. All active/inactive lifecycle evidence, focus return, stable parking, cleanup, and zero-crash gates passed and are bound into the public receipt. | Repeat only if persistent readiness, panel/glyph geometry, foreground grant, close input, DPI/host identity, lifecycle auditing, checkout-dialog ownership, sizing behavior, or the exact candidate changes, or a later audit regresses. | [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [v0.2.11 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.11), [current checkpoint](current-work.md#exact-release-evidence) |
| `WIN-PERSISTENT-READINESS-003` | `SETTLED` | Immutable unpublished `v0.2.12` exposed an exact-pre-dispatch frame-replacement race. Exact `v0.2.13` repaired it without weakening the input boundary but later exposed separate Friends close-glyph coordinate-phase and black-navigation defects. Published `v0.2.14` contains both repairs and its protected persistent profile completed all three shown/closed/parked cycles; checkout, shortcut, and managed profiles completed the ordered 31/31 receipt with 27 active routes and zero crashes. | Repeat after exact-frame readiness retry, invalidation auditing, close-glyph/panel classification, Steam UI, or the exact candidate changes. Do not rerun or move `v0.2.12`/`v0.2.13` or splice their roots. | [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [v0.2.14 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.14), [current checkpoint](current-work.md#exact-release-evidence) |
| `WIN-FRIENDS-READINESS-001` | `SETTLED` | Exact `v0.2.13` stopped fail-closed because the coarse glyph scan skipped a valid opposite-phase Friends close glyph and a generic brightness check rejected its intentionally black navigation strip. Published `v0.2.14` preserves scan phase and accepts only a directly sampled threshold-passing glyph as alternate chrome evidence while retaining mandatory modal geometry, backdrop, loaded content, foreground, and exact-frame gates. The protected managed Friends route and all other cases passed in the ordered 31/31 receipt with 27 active routes, Steam continuity, independent audits, cleanup, and zero crashes. | Repeat after screenshot coordinate-phase handling, glyph scoring, Friends/chrome classification, exact-frame dispatch, Steam Friends UI, or the candidate/proof schema changes. Never click an unproved target or reuse rejected roots. | [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [v0.2.14 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.2.14), [current checkpoint](current-work.md#exact-release-evidence) |
| `WIN-SHORTCUT-ROUTES-001` | `SETTLED` | Protected `v0.1.4` completes all ten shortcut routes in the exact receipt order. Every route has challenge-bound foreground grant, activation, scale-aware glyph-bound close, inactive callback, parking, focus return, complete presenter/native-host/renderer D3D11 agreement, clean crashes, cleanup, unchanged candidate binding/ACL, and one continuous Steam identity. The formerly failing 100%-scale user route passes and route ten launches and completes. | Repeat only if shortcut target mapping, scale-aware glyph detection, broker/grant, managed lifecycle, D3D11 diagnostics, Electron/Steam, candidate binding, or receipt semantics changes, or a later audit regresses. Do not reuse focused or rejected roots. | [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [presenter plan](native-overlay-presenter-plan.md#windows-position), [current checkpoint](current-work.md#current-state) |
| `WIN-RAW-BASELINE-001` | `DIAGNOSTIC ONLY` | The aggregate `full` suite's legacy raw web case injected Steam into the correct public process but created no managed presenter surface, so `IsOverlayEnabled` stayed false and the suite stopped before product routes. This is not a D3D11 managed-route regression. | Only if the raw baseline implementation is changed to own a supported presentation surface or a deliberate diagnostic profile changes. Use `managed`, `shortcut-routes`, and public `checkout` for current product proof; do not rerun the unchanged aggregate as the gate. | [latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-WGL-001` | `DIAGNOSTIC ONLY` | The older WGL and direct-Chromium paths produced blank/black surfaces or unreliable close behavior. They are comparisons, not product paths. | D3D11 regresses or a deliberate alternative-backend change materially alters WGL/direct hooking. | [Windows position](native-overlay-presenter-plan.md#windows-position) |
| `WIN-ONEPROCESS-001` | `DIAGNOSTIC ONLY` | The experimental Windows one-process control server raced overlay readiness and caused Steam/webhelper churn. Process-per-case remains the accepted proof shape. | A readiness-gated redesign exists with explicit lifecycle and cleanup tests. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-CHECKOUT-PUBLIC-001` | `SETTLED` | Exact `v0.1.4` receipt root `windows-v0.1.4-receipt-checkout-20260716-033000` passes all four public cases: prepare-only, direct synthetic approval, shortcut approval, and shortcut open-and-wait. The three active cases have complete D3D11 lifecycle, glyph-bound close, park, focus return, zero crashes, and exact broker/cleanup/Steam evidence. No private `InitTxn` request was used. | Repeat only if public checkout target resolution, broker/grant, shortcut routing, managed lifecycle, D3D11 diagnostics, Steam/Electron, or receipt semantics changes, or a later audit regresses. App ID `480` remains synthetic routing only, not real purchase proof. | [current checkpoint](current-work.md#current-state), [Windows matrix](../../scripts/windows-overlay-matrix.ps1), [summary auditor](../../scripts/summarize-windows-overlay-matrix.cjs), [v0.1.4 Release](https://github.com/jstroh/steam-bridge/releases/tag/v0.1.4) |
| `WIN-CHECKOUT-WEB-001` | `SETTLED` | A private configured-product web-session InitTxn returned a Steam URL, opened through the managed D3D11 overlay, closed cleanly, returned focus, and kept crash diagnostics clean. This does not claim a client-session authorization callback. | Checkout target resolution, managed lifecycle, Steam Web API contract, or configured-app launch lane changes. Otherwise retain this as the recommended Windows Electron path. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |
| `WIN-CHECKOUT-CLIENT-001` | `ENVIRONMENT BLOCKER` | Public App ID `480` has no configured products or usable real `InitTxn` lane, so its checkout matrices prove only synthetic approval routing. The historical explicit-client target/no-prompt result remains inconclusive. A 2026-07-10 UTC current-package attempt passed package, Session 1, readiness, native-load, render-health, and crash gates, but its sanitized preflight request was not a complete non-empty product-proof shape; the matrix stopped before direct `InitTxn` and produced no transaction or callback evidence. | Only when the configured game, `InitTxn`-capable application/backend path, and complete private request/runtime handoff exist, run exactly one current-build private direct client-session proof with managed-operation, correlated callback, close, crash, and QueryTxn gates. Device/package health and App ID `480` are not rerun triggers. Do not lengthen timeouts, use a shortcut callback case, or broaden into the public matrix. | [Paused purchase work](current-work.md#active-goal), [live evidence](cross-platform-overlay-status.md#latest-windows-evidence), [smoke coordinator](../../examples/electron-basic/checkout-proof.cjs), [managed helper](../../packages/steam-bridge/src/index.ts) |
| `WIN-CLIENTQUERY-001` | `ENVIRONMENT BLOCKER` | Bounded read-only QueryTxn diagnostics use closed schema `1`; the Windows auditor normalizes again and requires one attempted query between wait-timeout and prompt-missing events. The 2026-07-10 UTC suite stopped before direct `InitTxn`, so it ran no query and added no live diagnostic result. | Only within the eventual single direct client-session proof after the configured app, `InitTxn`-capable application/backend path, and complete private request/runtime handoff exist—and only after that run captures a transaction but misses its prompt. Never run QueryTxn as a separate probe or finalize/capture as diagnosis. | [Paused purchase work](current-work.md#active-goal), [closed schema](../../examples/electron-basic/checkout-proof.cjs), [Windows auditor](../../scripts/summarize-windows-overlay-matrix.cjs) |
| `WIN-CHECKOUT-DEFAULT-001` | `SETTLED` | Omitting `usersession` returned a sanitized failure before any transaction/target for the tested configured product/account. Explicit web versus explicit client is the useful comparison. | Request schema, product/account configuration, or Steam's documented default-session behavior changes. | [Latest Windows evidence](cross-platform-overlay-status.md#latest-windows-evidence) |

| `WIN-STANDALONE-OVERLAY-PACING-001` | `SETTLED` | The first exact local `0.3.6` FOV4 tarball held game paint/present at 59.9 FPS against 60 Hz, but a real Steam checkout overlay exposed retained-frame pump/present medians of 47.7 FPS. Steam owns the visible overlay frame and may park Electron paint at zero, so overlay success is native-present cadence rather than forced hidden-renderer painting. The scheduler had applied its early DXGI wake-up only when `continuousPresent` was requested at construction, not when Steam activation enabled the same native continuous-present state at runtime. Keying the Windows standalone early wake-up from the applied state repaired the exact rebuilt tarball to 60.0 FPS median native pump/present across 32 overlay samples, with a 57.6 activation minimum and zero device losses, recoveries, latency timeouts, or slow copies. The candidate receipt now requires game paint plus present and overlay present to reach 95% of the display target while still reporting overlay paint. Exact protected `v0.3.21` candidate proof repeated the real configured game and ordinary Friends overlay at 125% DPI: 551 game samples and 21 active-overlay samples both held a 59.9 FPS median presentation rate against 60 Hz, with zero device losses, recoveries, wait timeouts, slow copies, unsynchronized samples, crashes, or stderr. | Repeat after session timer scheduling, overlay-driven continuous-present policy, DXGI frame-latency gating, display-rate synchronization, or proof pacing semantics change, or overlay present cadence regresses. Do not keep a post-`Present` full-frame JS delay and do not fail an overlay merely because Steam parked the obscured Electron renderer. | [session scheduler](../../packages/steam-bridge/src/index.ts), [receipt generator](../../scripts/windows-live-proof-receipt.cjs), [current checkpoint](current-work.md#2026-08-08-v0321-stable-release-checkpoint) |
| `WIN-RELEASE-PROOF-AUTOMATION-001` | `SETTLED` | A one-time physical Shift+Tab on exact `v0.3.6` proved the native shortcut route, but making a person repeat it for every immutable candidate is a rejected release-harness dependency. FOV4 exposes `Steam Friends Overlay (QA)` only under `STEAM_BRIDGE_QA_OVERLAY=1`; the native View command calls the ordinary non-purchase Friends dialog and records a closed marker. Receipt schema 5 retains that marker, declares no human input, requires qualified game/overlay presentation at 95% of the selected display, and validates the full asynchronous shared-texture safety shape. The exact packaged `v0.3.33` candidate held 164-165 FPS at 165 Hz through gameplay, the real Friends overlay, resize, minimize, focus, and fullscreen transitions with zero wait/copy timeouts, fatal timeouts, submission failures, saturation drops, device loss, or recovery. Four asynchronous fence completions crossed the old slow threshold only during fullscreen storage recreation (22.211 ms maximum) without reducing cadence. Schema 5 therefore rejects every real failure/drop and any counter regression, but permits only the greater of eight or 0.1% non-blocking slow completions; schema 4's unconditional zero was valid only for the removed synchronous blocking path. `v0.3.33` remains rejected/unpublished, and a fresh `v0.3.34` exact candidate must independently pass the new receipt. | Repeat after the FOV4 QA command, native menu dispatch, ordinary-dialog activation, overlay lifecycle/close, FPS telemetry shape/interval, asynchronous copy completion/saturation meaning, receipt schema, wait-timeout meaning, target-transition meaning, or a packaged cadence regression. Do not ask a human to press Shift+Tab for routine proof, splice evidence across immutable candidates, or restore the synchronous zero-slow-copy invariant to the asynchronous fence path. | [FOV4 menu gate](../../../fov4-steam/main/main.js), [receipt generator](../../scripts/windows-live-proof-receipt.cjs), [current checkpoint](current-work.md#2026-08-13-asynchronous-copy-release-proof-correction-0334-candidate) |
| `WIN-FRAME-PUMP-MICROTASK-001` | `SETTLED` | Deferring each dirty offscreen frame through a full `setImmediate` turn reduced settled 165 Hz Windows presentation to about 143.6 FPS. The standalone inactive frame pump now coalesces with one `queueMicrotask`, revalidates lease/session/overlay ownership before presenting, and leaves the timer as retained-frame and active-overlay fallback. The actual FOV4 game then held 164.8 FPS baseline, 164.75 FPS with the real Steam overlay active, and 164.4 FPS after close against 165 Hz, with zero device loss, recovery, or slow-copy events. | Repeat after Windows dirty-frame scheduling, continuous-present ownership, overlay activation, DXGI gating, or event-loop ownership changes, or 165 Hz cadence regresses. Do not restore a full-turn dirty-frame delay or run competing timer and frame pumps. | [session pump](../../packages/steam-bridge/src/index.ts), [current checkpoint](current-work.md#2026-07-28-windows-final-source-linked-qa) |
| `WIN-DXGI-ASYNC-WAIT-001` | `SETTLED` | An actual-game human movement trace reproduced visible hitching while Chromium stayed at 60 FPS/16.8 ms and native work stayed inside budget; DXGI frame statistics alone recorded repeated refreshes. A full flip queue now duplicates its waitable handle, waits in a blocking worker, and grants the matching auto-reset signal as a generation-bound one-shot render permit, so Electron's Win32 message thread never sleeps or spins. A 60-second physical-input A/B rejected both timer-polled and worker-driven maximum-latency-one variants. The retained maximum-latency-two worker build serviced all 368 queue-full events, delivered 3,546 Presents across 3,550 refreshes (59.83 FPS), recorded four isolated repeats, and had zero post-boundary paint/shared-texture intervals, native pump durations, Presents, or renders above 25 ms and zero device loss. A follow-up review restricts async waits to dirty D3D11 surfaces with a retained source, so startup readiness and the OpenGL diagnostic backend cannot create a false-resolving microtask loop; lifecycle coverage locks timeout retry, coalescing, wake-up, and close behavior. | Repeat after Windows presenter scheduling, swap-chain depth, wait-handle ownership, surface/renderer generations, Electron offscreen cadence, or display-rate selection changes, or a fresh report reproduces the periodic hitch on this worker build. Never block the Electron message pump, restore the 1 ms readiness loop, or switch to maximum latency one without this exact external-source A/B. | [D3D renderer](../../crates/native/src/windows_d3d11.rs), [session scheduler](../../packages/steam-bridge/src/index.ts), [current checkpoint](current-work.md#2026-08-02-windows-movement-stutter-root-cause-and-pacing-checkpoint), [Microsoft waitable swap-chain guidance](https://learn.microsoft.com/en-us/windows/uwp/gaming/reduce-latency-with-dxgi-1-3-swap-chains) |
| `WIN-WORKAREA-RECONCILE-001` | `SETTLED` | A live `1920x1200` to `1280x800` change could leave a remembered 1280x720 logical client wider than the new work area. The same standalone HWND now reconciles on DPI, display, and work-area notifications: it centers/clamps the outer frame without changing the remembered logical client, exposes `displayWorkAreaClamped`, and restores the requested 1280x720 logical client when room returns. Actual-game proof covered a fully visible 1280x752 outer frame, 1264x693 clamped client, aligned real overlay, 59.95/60.0/60.002 present/paint/rAF medians, and exact 1600x900 physical / 1280x720 logical restoration at 125% scale. | Repeat after standalone window geometry, DPI/display/work-area handling, minimum-size policy, menu metrics, or the diagnostic changes, or low-resolution restore regresses. Keep one host; never solve this with popup recreation or by overwriting the remembered logical client. | [native host](../../crates/native/src/native_surface.rs), [current checkpoint](current-work.md#2026-07-28-windows-final-source-linked-qa) |
| `WIN-DISPOSABLE-FOCUS-HELPER-001` | `CLOSED PATH` | Spawning a blank auxiliary application solely to manufacture focus loss caused the external Windows capture harness to exhaust D3D capture resources and destabilized the QA controller after the real focus case was already green. It produced no valid product finding. Final qualification must use an existing operator-approved focus target or direct native focus-state evidence and must not create a disposable UI process or capture that unrelated window. | Reopen only if the QA controller gains an isolated, resource-bounded focus target with explicit cleanup proof. Never treat controller OOM or a disposable helper crash as Steam Bridge product evidence. | [current checkpoint](current-work.md#2026-07-28-windows-final-source-linked-qa) |

## Steam Deck and Linux x64

| ID | Status | Finding | Repeat only when | Evidence |
| --- | --- | --- | --- | --- |
| `DECK-WAYLAND-HOST-001` | `SETTLED` | A native-Wayland Electron handle is not an X11 window ID and must never be reinterpreted as one. Current Steam Deck Desktop uses one explicitly selected, bounds-backed Xwayland/GLX top-level host. A session-local KWin script pairs it to the same-process Electron source and mirrors compositor client geometry, fullscreen state, shell exclusions, and the active-host minimize transition; Electron visibility hooks unmap it for minimize/hide and remap the same XID after restore. The native host is undecorated, input-empty and transparent while parked, clips only restored bottom corners, remains rectangular in fullscreen, and stays transparent during passive notification needs-present pulses so it cannot black out the Wayland game behind Steam's separate toast. Requested Wayland `session` mode is safely implemented by the pre-attached persistent host and exposed as `effectivePresenterMode=persistent`, because live on-demand GLX creation inside Steam's injected activation stack crashed. The 2026-07-19 Desktop core, transition, targeted toast, and final minimal passes verified move, resize, fullscreen, minimize/restore, focus, title-bar exclusion, corner shape, one-host reuse, active/inactive lifecycle, passive UI preservation, and clean crashes. | Electron native-handle semantics, Wayland/Xwayland selection, GLX host creation, KWin scripting APIs, client-geometry/fullscreen/minimize handling, opacity/input/shape behavior, notification presentation, SteamOS/Steam/Electron, or presenter lifecycle changes, or a focused route regresses. Do not revive the rejected child-window, raw Wayland-handle-as-XID, or on-demand injected-stack GLX paths. | [current Deck requalification](cross-platform-overlay-status.md#2026-07-19-steam-deck-requalification), [native Linux host](../../crates/native/src/native_surface.rs), [Electron integration](../../packages/steam-bridge/src/electron.ts), [KWin synchronization](../../packages/steam-bridge/src/kwin.ts) |
| `DECK-COLD-START-001` | `SETTLED` | On Deck Desktop, Steam may return `IsOverlayEnabled() = true` before the Linux `gameoverlayui` helper is safe to call. An immediate managed web activation during that interval produced a reproducible native null-address crash while Steam's console showed the helper starting. The standalone Wayland/Xwayland path now defaults to a 3000 ms activation warmup from controller creation. Readiness status remains `overlay-not-ready` with `canWait=true`; sync `open*IfAvailable()` returns without invoking Steam, while `open*AndWait()` reserves the operation against duplicates and force-polls through the warmup without activating the presenter. The host remains transparent, input-empty, and at zero FPS until readiness is proven; already-ready operations retain a synchronous presenter start. A first-launch proof after a Desktop -> Game -> Desktop cycle recorded about 2.87 seconds remaining, waited to zero, then opened and closed a centered web surface with focus return, stable parking, and no crash. The built-in wait lifecycle, 6/6 Desktop matrix, and 2/2 Game matrix passed against that refreshed runtime. A later source review closed a checkout-only status bypass with complete unit proof; `.tmp-deck-duplicate-exact-rerun2-20260719` then passed the exact candidate with one target, duplicate suppression, visible activation, authenticated Escape close, focus return, stable zero-FPS parking, and no crashes. | Steam client/helper startup ordering, controller creation timing, readiness/wait semantics, the standalone Linux host selection, SteamOS/Steam/Electron versions, or activation APIs change, or a cold first launch regresses. Repeat from a real session transition or Steam cold start; a warm retry is insufficient. | [current Deck requalification](cross-platform-overlay-status.md#2026-07-19-steam-deck-requalification), [managed readiness](../../packages/steam-bridge/src/index.ts), [Desktop matrix](../../scripts/steam-deck-overlay-matrix.sh) |
| `DECK-MANAGED-001` | `SETTLED` | Deck Desktop's X11/GLX managed presenter has broad proof for web/store/Friends/dialog/profile/community/stats/achievements/user routes, shortcuts, passive toasts, synthetic checkout routing, close/back-to-app, and zero-FPS parking. Fresh current-package focused web proof retained one overlay target, active/inactive callbacks, close, focus return, stable parking, and clean crash evidence. Fresh progress and unlock notifications each rendered a visible public toast through Steam's overlay target, completed their required callbacks, remained non-modal, and found the passive transparent/click-through presenter already parked at 0 FPS without a `BOverlayNeedsPresent()` transition. Callback completion plus state-driven parking is therefore the Deck notification completion contract; the render path is recorded as `steam-overlay-target`, while platforms that emit a needs-present transition retain that path. | Linux presenter/isolation/input code, notification callback or parking contract, SteamOS/Steam client, Electron, route mapping, or exact evidence schema changes, or the current full matrix regresses. Prefer a focused suite for a localized change. Do not lengthen the notification wait merely because Deck omits a needs-present transition. | [Overlay proof matrix](cross-platform-overlay-status.md#overlay-proof-matrix), [Deck summary auditor](../../scripts/summarize-steam-deck-overlay-matrix.cjs) |
| `DECK-GAME-001` | `SETTLED` | Current Game Mode uses a separate two-case contract instead of the Desktop managed-route matrix. It passes Steam launch/injection, Deck and Big Picture identity, overlay readiness, passive X11/GLX managed presenter readiness at 0 FPS, compositor-native store `active=true`, Gamescope capture, exact SteamUI Escape `active=false`, app focus return, clean crashes, and zero-remnant cleanup while the local Wayland Gamescope session and Steam launcher remain active. A fresh managed-web control again created the X11/GLX host but produced no active callback or overlay target, so presenter-backed web remains a settled Game Mode non-activation boundary rather than a reason to run the Desktop 26-case suite. | Game Mode compositor/session routing, SteamUI store/back behavior, `gamescopectl`, managed web activation, Electron/Steam client, or the dedicated Game Mode manifest/summary contract changes, or either two-case gate regresses. Do not retry the unchanged presenter-web route, Spectacle capture, Guide close, or unregistered virtual-controller B input. | [Game Mode matrix](../../scripts/steam-deck-overlay-matrix.sh), [Deck runner](../../scripts/steam-deck-smoke.sh), [latest Deck evidence](cross-platform-overlay-status.md#current-evidence) |
| `DECK-RAW-001` | `DIAGNOSTIC ONLY` | Raw Steam social/dialog/hotkey behavior remains unreliable; disabling child isolation creates duplicate hook targets. The generic overlay URI produced a black presenter and crash evidence. Managed shortcuts and web-backed routes are the product path. | A specific raw-route implementation changes or Valve changes Deck overlay behavior. Do not retry unchanged focus/raise/opaque-host variants. | [Steam Deck shortcut gate](cross-platform-overlay-status.md#steam-deck-shortcut-gate) |
| `DECK-LAUNCH-001` | `SETTLED` | Steam must launch the full non-Steam shortcut game ID, not `480` or the internal 32-bit shortcut ID. App ID `480` proves generic plumbing only; real purchases require the actual installed app. | Shortcut serialization/runner logic changes or SteamOS changes shortcut IDs. | [Steam Deck shortcut gate](cross-platform-overlay-status.md#steam-deck-shortcut-gate) |
| `DECK-GAME-REMOTE-LAUNCH-001` | `CLOSED PATH` | In the current live Gamescope session, remote URI forwarding, app-launch, desktop-opener, Steam FIFO, wrapper, and `steam-runtime-steam-remote` invocations all returned without launching the installed non-Steam candidate. Synchronizing `DISPLAY` and the session bus into the systemd user activation environment did not change the result. Steam remained healthy and the candidate remained closed. The official installed product is a distinct proven path: a temporary quoted product launch option can own the exact local candidate, after which `steam://rungameid/2957110` launches it under the registered product identity. Restore the originally empty launch option after QA. | Repeat the failed forms only if SteamOS/Steam's non-Steam Game Mode remote-command transport materially changes. Do not cycle through the same command spellings, expose the private shortcut identifier, or reinterpret a direct process launch as Steam-injected evidence. Prefer the registered-product route for exact local candidate qualification. | [current checkpoint](current-work.md#active-goal-cross-platform-exhaustive-actual-game-qa) |
| `DECK-GAME-CEF-DEBUG-001` | `CLOSED PATH` | Starting Steam with the CEF remote-debug marker/wrapper caused both FOV4 and the independently known-good raw Store smoke to reach SteamUI with `IsOverlayEnabled=true` but never emit `GameOverlayActivated active=true`. Removing the marker/wrapper, restarting normal Steam, and rerunning only the failed Store case restored visible activation, Escape close, `active=false`, focus return, and zero crashes. This was an invalid test environment, not a Bridge or product regression. | Reopen only if Valve explicitly supports CEF-debug startup as equivalent Game Mode overlay evidence and an independent control proves it. Never enable Steam CEF debugging during Game Mode qualification. | [current checkpoint](current-work.md#active-goal-cross-platform-exhaustive-actual-game-qa), [Game Mode matrix](../../scripts/steam-deck-overlay-matrix.sh) |
| `DECK-ACTUAL-GAME-001` | `SETTLED` | A 2026-07-23 focused Steam Deck Desktop Mode pass against a real Steam-launched consumer game package proved the current Linux/Electron integration. The package-level Linux launcher starts the real Electron binary with `--no-zygote --no-sandbox` before Chromium's first zygote, closing the Steam-injected zygote crash without keeping local wrapper code in the consumer app. The app-owned Deck BrowserWindow starts fullscreen at the current display bounds, producing first overlay snapshots at `1280x800` with no transient `800x600` or tiny top-left Steam surface. A temporary CDP QA harness drove DOM buttons because fixed Wayland/XTest coordinates were unreliable and a Shift+Tab XTest attempt opened KWin's switcher; that harness was removed after the run. The live game reached a Babylon.js WebGL2 canvas at rect `[0,0,1280,800]`, cursor hiding returned `true` with computed cursor `none`, programmatic app IPC opened Steam's fullscreen overlay shell over the live canvas with `active=true` and bounds `1280x800`, Escape emitted `active=false` and returned to the game, and FPS measured 90.094 before overlay, 90.030 during overlay, and 90.084 after close against KWin's 90.004 Hz output. | Electron process-start launcher, Linux child-process isolation, Deck BrowserWindow startup sizing/fullscreen policy, app cursor IPC, preload overlay IPC, Wayland session routing, Steam overlay route choice, Chromium/Electron/SteamOS/Steam versions, or actual-game bootstrap changes, or a focused Deck actual-game case regresses. Keep Plasma Wayland, LF-only wrappers, DevTools closed, one Steam client active across machines, and do not use fixed compositor coordinates as product proof. | [current Deck actual-game checkpoint](current-work.md#current-state), [Linux helper](../../packages/steam-bridge/src/electron-builder.ts), [Electron integration](../../packages/steam-bridge/src/electron.ts) |
| `DECK-WAYLAND-ACTIVE-RESIZE-001` | `OPEN` | A 2026-07-23 focused actual-game check found that interactive resize can activate and raise the Electron source above the active paired Xwayland/GLX overlay host. Read-only X11 sampling proved that the mapped, opaque host continued drawing changing Steam frames; the visible black region was the source WebGL surface occluding it, not a stopped Steam presenter. The repair candidate keeps the established compositor-paired host and KWin as the sole live geometry writer: resize-time restacks raise without activation; an authenticated transparent-to-opaque presentation edge intentionally activates the host once; and the opaque-to-transparent edge returns focus only if the host is still active and the source is live, visible, and unminimized, preserving an unrelated Alt+Tab target. Native GLX coalesces `ConfigureNotify` and derives the viewport from the configured drawable, while a one-shot Electron content-bounds seed reconciles geometry after fullscreen exit. No live closure proof is claimed yet. | Focused-retest active-overlay move, incremental and rapid resize, terminal minimum, direction reversal, fullscreen exit, alignment, overlay close, focus return, Alt+Tab away/back, keyboard/Escape input, and source-WebGL recovery after the candidate is deployed. Run the complete Deck pass only after these affected cases are green. Do not use a popup, `keepAbove`, resize-time recreate/remap, fixed coordinates, or synthetic Shift+Tab as a fallback or as product evidence. | [current Deck resize checkpoint](current-work.md#current-state), [KWin synchronization](../../packages/steam-bridge/src/kwin.ts), [native Linux host](../../crates/native/src/native_surface.rs) |
| `DECK-HOST-001` | `SETTLED` | A Windows-hosted Linux package must be validated as readable locally and have the app, helper, Crashpad handler, and sandbox executable bits restored after transport. Direct SSH runs must inherit the active Deck X11/session environment. Before a same-shortcut relaunch, the runner retires only its exact packaged executable, including a still-running replaced image marked deleted by Linux, its own inhibitor, and overlay helpers whose target is missing, zombie, or exited; Steam and live game targets remain untouched. The same runner owns explicit matrix cleanup so receipts do not duplicate process ownership logic. Desktop capture uses Spectacle, while active Game Mode uses state-driven `gamescopectl screenshot`; focus/state probes select the Game X display when Gamescope is active. Host state captures retain process identity/parentage and redacted env-key presence rather than full command lines or values, and helper output redacts standalone Steam identifiers. These changes passed runner/matrix self-tests, Deck-native helper self-test, direct launch, repeated focused launches, and clean Desktop and Game Mode receipts. | Package transport/filesystem semantics, Electron runtime helper names, Deck session/capture discovery, exact runtime identity/cleanup, Steam shortcut relaunch behavior, or artifact privacy rules change, or preflight/cleanup regresses. Do not restart Steam or rerun a retained shortcut until the bounded exact-runtime cleanup fails. | [Deck host runner](../../scripts/steam-deck-smoke.sh), [Linux packaged helper](../../scripts/linux-electron-smoke.sh), [Steam Deck shortcut gate](cross-platform-overlay-status.md#steam-deck-shortcut-gate) |
| `DECK-PRESENTATION-HANDSHAKE-002` | `OPEN` | The unreleased Deck Desktop repair keeps the existing persistent compositor-paired host and KWin-only geometry ownership. Activation is now prepare-inert -> opacity -> native commit -> two focus-confirmed pumps; cancellation and timeout remain transparent/input-empty for that intent epoch. Transport degradation publishes its final role marker only after an atomic native input/opacity park and readback, then requires two stable mapped drawable samples. Fullscreen requires a later Configure; rapid reversal accepts an exact unchanged origin but fences a queued stale Configure until a later final-size Configure is stable twice. Focused session/presenter regressions are green, including terminal detach for native fullscreen failure before mutation and marker failure after mutation. This is implementation evidence, not live closure. | Run only the affected actual-game Deck Desktop cases until each is green: active move, slow/rapid/reversing resize, minimum size, fullscreen/restore and resolution change, minimize/restore, focus/Alt+Tab, input/close/focus return, seams, host identity, FPS/Hz, and crashes. Then run one complete Deck pass before release. Never substitute popup/topmost/`keepAbove`, resize-time recreate/remap, `setBounds`, fixed coordinates, synthetic Shift+Tab, or DevTools. | [current checkpoint](current-work.md#active-goal-deck-desktop-presentation-and-input-safety), [KWin synchronization](../../packages/steam-bridge/src/kwin.ts), [presenter/session policy](../../packages/steam-bridge/src/index.ts), [native host](../../crates/native/src/native_surface.rs) |
| `DECK-INPUT-001` | `SETTLED` | On current Deck Desktop/KWin, one visually detected Steam X closed an ordinary presenter-backed web surface and passed inactive callback, app-focus return, parking, and crash checks, but a final built-in wait probe found that the X can render before the browser finishes loading and an early detected-X click can be ignored. Fixed coordinates remain rejected. Store `openAndWait` also ignored two detected-X attempts; one XTest attempt activated KWin Overview, and a device-level click at the detected physical point did not close it. The same retained routes closed immediately through the managed Shift+Tab path and passed the full `openAndWait` completion, focus, stable parking, and crash contract. The Desktop release matrix therefore uses managed Shift+Tab as the deterministic close contract and keeps detected-X input as a focused visual diagnostic only. In Game Mode, the compositor-native store advertises controller Back but a virtual controller never registered with Steam Input; Guide only changed shell chrome. Exact Escape on SteamUI's Xwayland display emitted `active=false`, restored the focused fullscreen app, and is now the dedicated Game Mode close contract. | Steam client store surface, KWin or Gamescope input behavior, close-glyph rendering/detection, managed shortcut pass-through, SteamUI Escape routing, or store lifecycle changes, or an accepted close path regresses. Do not tune coordinates, add clicks, or retry the rejected input paths on unchanged routes. | [Deck host runner](../../scripts/steam-deck-smoke.sh), [Deck matrix](../../scripts/steam-deck-overlay-matrix.sh), [Overlay proof matrix](cross-platform-overlay-status.md#overlay-proof-matrix) |
| `DECK-FRAME-PACING-001` | `SETTLED` | Electron reports a zero display frequency in the current KWin Wayland session, so the smoke diagnostic matches the active display geometry to KWin `supportInformation` and records the source. At 90.004 Hz, the exact no-presenter baseline measured 90.000 renderer FPS. Two active Steam-browser samples measured 86.682 and 86.844 FPS with 11.1 ms p50 and sparse tail stalls. After close, the presenter remained at `currentFps=0` and `pumpCount=2567` across both samples while renderer FPS measured 86.350 then 83.358. The passive presenter is therefore conclusively parked; remaining sparse stalls occur while Steam's injected `gameoverlayui` remains attached until process exit. | Electron/Chromium, KWin output reporting, Steam overlay injection, presenter scheduling, renderer FPS instrumentation, or display mode changes, or passive `pumpCount` changes after close. | [current Deck requalification](cross-platform-overlay-status.md#2026-07-19-steam-deck-requalification), [smoke FPS diagnostic](../../examples/electron-basic/main.js) |

### 2026-07-25 Linux architecture supersession

`DECK-WAYLAND-ACTIVE-RESIZE-001` and `DECK-PRESENTATION-HANDSHAKE-002`
describe the retired two-window paired-host candidate and are superseded. Do
not execute their popup/companion pairing matrix against the current product.

| ID | Status | Finding | Repeat only when | Evidence |
| --- | --- | --- | --- | --- |
| `DECK-APPLICATION-HOST-001` | `SETTLED` | The real Steam-launched consumer now has one visible X11/GLX application host and one hidden Electron offscreen renderer. Electron's one-plane BGRA dma-buf is imported through XCB DRI3 and `GLX_EXT_texture_from_pixmap`, then sampled through a shader/FBO into a retained texture before the producer is released. The exhaustive actual-game pass proved exact 1280x718 windowed, 1280x800 fullscreen, 640x480 minimum, maximize/restore, move/reversing-resize, minimize at 1 FPS and restore at 90 FPS, Alt+Tab/focus return, native keyboard/Escape, cursor hiding, web-overlay open/duplicate/close, retained presentation throughout overlay-active state transitions, one surviving process/host, roughly 90 FPS against 89.869 Hz, and zero import failures. A later focused packaged-candidate pass proved the directly clickable three-group application menu survives live-game handoff, reserves exactly 26 DIPs, keeps an undistorted 1280x692 canvas at y=26, and preserves 89.667/90.000/90.003 FPS before/during/after overlay close through native EIS Escape. The 2026-07-28 Electron 43.2.0 client-px pass exposed modifier `0` as a compatibility edge: Linux defines it as the linear modifier, but the native guard rejected it and presenter/rAF cadence masked a retained, non-updating game frame. The unreleased repair accepts linear zero while continuing to reject tiled/vendor modifiers. Its Deck-native test and focused Steam-launched actual-game retest produced 89-91 shared imports per second, the named DRI3/GLX backend, increasing import counts, zero failures, 90 FPS native presentation, retained-overlay presentation at 90 FPS, and 90 FPS import recovery after Escape. A later Game Mode product run exposed and closed a second GLX metadata edge under `DECK-GLX-Y-INVERTED-001`; state-driven Gamescope composite capture now supplies authoritative pixel evidence. | Electron native-pixmap metadata or modifier semantics, XCB DRI3/GLX import, retained copy, single-host window lifecycle, cursor/input, scheduler, Steam/Electron/Mesa, or actual-game bootstrap changes, or a focused case regresses. Require independent source-frame/import progress and compositor pixel evidence before overlay activation; page rAF or presenter cadence alone is insufficient. Retest only the affected case; run the complete Deck pass again only after all affected cases are green and immediately before a release candidate. | [current checkpoint](current-work.md#settled-linuxdeck-application-host), [native host](../../crates/native/src/native_surface.rs), [session](../../packages/steam-bridge/src/index.ts) |
| `DECK-SHUTDOWN-HOOK-CLEANUP-001` | `SETTLED` | A configured-product Deck process could abort during ordered Steam shutdown with `__cxa_pure_virtual`. Native backtrace pinned the call to Steam Bridge removing the deprecated post-API-result process hook even though no hook had been registered. Empty process-hook cleanup is now a no-op on Linux only; macOS and Windows retain the SDK reset required for their process-global callback state. A focused macOS regression proved that making the Linux no-op cross-platform can terminate the process on first overlay activation. A separate same-pump edge could capture X11 `close` and then throw on `DestroyNotify` before JavaScript drained the close event; the session now dispatches captured input before propagating that terminal pump error. The rebuilt stable-Electron candidate completed File -> Exit, `SIGTERM`, and compositor-native close without an abort, left no app process, kept Steam alive, and warm-relaunched. The compositor close proves the captured close survives terminal drawable teardown. | Repeat after process-hook registration/cleanup, Steam shutdown ordering, native pump/drain ordering, X11 close handling, Electron/Steam client versions, or app signal routing changes, or if clean quit leaves a process or aborts. Do not remove an unregistered deprecated Steam hook on Linux, do not suppress the empty-state SDK reset on macOS or Windows, and do not discard captured input solely because the same native pump became terminal. | [native hook cleanup](../../crates/native/src/compat.rs), [session pump](../../packages/steam-bridge/src/index.ts), [session regression](../../tests/steam-bridge.test.cjs), [current checkpoint](current-work.md#2026-07-30-deck-shutdown-and-exact-candidate-requalification) |
| `DECK-GLX-Y-INVERTED-001` | `SETTLED` | In Gamescope Game Mode, the exact shared-texture candidate's compositor output was vertically inverted while the Electron source screenshot and forced CPU-upload control were upright. The GLX texture-from-pixmap importer had never queried the selected FBConfig's standard `GLX_Y_INVERTED_EXT` attribute. The repair queries and records that attribute, flips Y only in the imported-texture-to-retained-texture copy, and explicitly keeps CPU upload and final presentation unflipped. Deck-native Rust tests pass. The rebuilt registered-product candidate is visibly upright at 1280x800 in a state-driven Gamescope composite screenshot, with no bars or crop; focused baseline/Store-overlay/post-close rates are 90.003/90.000/90.003 FPS, callbacks are exactly `[true,false]`, and product focus returns after Escape. | Repeat if GLX FBConfig selection, texture-from-pixmap import, shader copy, retained-texture orientation, CPU upload, Gamescope/Mesa/Electron, or the new diagnostic changes, or orientation regresses. Never globally flip the final draw or CPU path. Require a compositor-owned screenshot, not only Electron source pixels. | [GLX specification](https://registry.khronos.org/OpenGL/extensions/EXT/GLX_EXT_texture_from_pixmap.txt), [native host](../../crates/native/src/native_surface.rs), [current checkpoint](current-work.md#active-goal-cross-platform-exhaustive-actual-game-qa) |
| `DECK-EGLIMAGE-GLX-001` | `CLOSED PATH` | EGL accepted the dma-buf and created an EGLImage, but targeting that image from the existing GLX context crashed Mesa/GL dispatch. Direct `glCopyImageSubData` from a GLX texture-from-pixmap also returned `GL_INVALID_OPERATION`. Both implementations were removed. The proven copy samples the GLX-bound pixmap into an FBO-backed retained texture. | Reopen only if the renderer changes to one coherent EGL context/API or Mesa documents and proves safe EGLImage interop with the active GLX context. Never use it as an automatic fallback. | [native host](../../crates/native/src/native_surface.rs), [current checkpoint](current-work.md#active-goal-deck-desktop-application-host) |
| `DECK-SINGLE-HOST-CLOSED-PATHS-001` | `CLOSED PATH` | Popup/topmost/`keepAbove` companions, resize-time recreate/remap, nested child GLX, hidden-root reparent, direct Electron desktop GL/Vulkan, and unmapped proxy dual-drawables all failed geometry, presentation, hook selection, or lifecycle requirements. A direct service launch can also bypass real Steam injection and is diagnostic only. | Reopen only after a material Steam hook-selection or renderer-architecture change with visible pixel proof. Launch product evidence through Steam's game URI. | [current checkpoint](current-work.md#active-goal-deck-desktop-application-host) |

## macOS Apple Silicon

| ID | Status | Finding | Repeat only when | Evidence |
| --- | --- | --- | --- | --- |
| `MAC-QA-SYNTHETIC-TITLE-REVERSAL-001` | `SETTLED` | Earlier focused runs intermittently lost one of two separately synthesized opposing title drags. The retained helper uses one login-session event source, the bounded mouse-down latch, and an exact eight-point first motion. Exhaustive root `macos-final-stable-43-2-0-dc8bb13-17` passed title reversal under all five required display profiles, as well as slow/fast title movement, child pairing, visual health, and cleanup. The earlier environment blocker is closed without changing the one-child architecture. | Input helper, AppKit/WindowServer, Electron, display-profile matrix, or child attachment semantics change, or reversal regresses. Never revive popup/companion experiments. | [stable candidate checkpoint](current-work.md#2026-07-28-stable-4320-final-candidate-checkpoint) |
| `MAC-QA-LOW-RETINA-SYNTHETIC-INPUT-001` | `SETTLED` | Earlier isolated low-Retina runs delivered zero trusted synthetic pointer events. With Steam and the candidate launched through the correct logged QA session, exhaustive root `macos-final-stable-43-2-0-dc8bb13-17` passed low-Retina cursor/baseline, semantic input, menu, display transitions, all drag/resize paths, minimum size, maximize/minimize/hide/focus/fullscreen, GPU recovery, overlay stress, and baseline/overlay FPS. Cleanup and exact display restoration passed. The earlier environment blocker is closed; the discarded login-session-source, process-group, and invalid passthrough-child hit-test experiments remain closed paths. | Input helper, macOS display/compositor behavior, Electron, PX input contract, or low-Retina profile changes, or the exhaustive low-Retina lane regresses. | [stable candidate checkpoint](current-work.md#2026-07-28-stable-4320-final-candidate-checkpoint) |
| `MAC-QA-FRESH-TELEMETRY-PREFLIGHT-001` | `SETTLED` | Final root `macos-final-stable-43-2-0-19bba4a-15` launched the exact candidate and reached the production game, but its append-only app log received no records from that launch. Downstream cases therefore reported `fps_unavailable` with empty native telemetry even though no product regression had been established. The invalid run was stopped, candidate processes were closed, and an independent display inventory matched the saved 3456x2234/120 Hz mode. The runner now starts at log EOF and requires a fresh structurally valid attached-host telemetry marker before the first case, failing distinctly as `telemetry_unavailable`. | Logging, launcher, shortcut, or telemetry-marker plumbing changes. If this gate fails, repair the profile precondition before running or diagnosing any case; historical log records never qualify. | [stable candidate checkpoint](current-work.md#2026-07-28-stable-4320-final-candidate-checkpoint) |
| `MAC-QA-MAX-FPS-FOCUS-001` | `SETTLED` | The exhaustive root's sole red was max-refresh baseline FPS losing foreground focus after otherwise healthy measurement: renderer scheduler 120 FPS, Chromium presentation feedback 119.668 FPS, no drops, stable renderer/layer tree, and no trace loss. The same case passed immediately before the exhaustive run, then exact supplemental root `macos-focused-max-transition-fps-18` passed live display transition, pacing transition, max-refresh FPS, cleanup, and restoration in the affected order. All other 124 exhaustive profile-case executions stayed green. This is closed as a transient focus interruption, not a cadence or child defect. | The exact max-refresh transition-to-FPS sequence regresses, focus acquisition changes, or Electron/macOS focus semantics change. Retest this sequence only; do not rerun the other green cases. | [stable candidate checkpoint](current-work.md#2026-07-28-stable-4320-final-candidate-checkpoint) |
| `MAC-BUILD-001` | `SETTLED` | Supported macOS output is arm64-only. The in-bundle launcher, renamed Electron executable, Steam overlay entitlements, and app/game/overlay ID setup are required. Intel, Rosetta, and universal builds are intentionally unsupported. An ordinary current-host package must prefer the fresh `steam_bridge_native.local.node` produced by `npm run native:build`; cross-target packages and explicit `--artifacts-dir` release assemblies retain target-named artifact precedence. The exact 2026-07-19 rebuild rejected the old precedence through its contract gate, then passed with all 1,130 methods after the precedence repair. | Packaging, native artifact selection, signing, launcher, Electron-builder integration, or platform-support policy changes. | [macOS Apple Silicon plan](native-overlay-presenter-plan.md#macos-apple-silicon-plan) |
| `MAC-POLL-001` | `SETTLED` | `BOverlayNeedsPresent()` crashed Steam's injected renderer on both OpenGL and Metal. macOS wrappers keep polling disabled and expose no opt-in. | Valve supplies a confirmed fix and an isolated crash-safe experiment is explicitly justified. Do not casually re-enable this poll. | [macOS Apple Silicon plan](native-overlay-presenter-plan.md#macos-apple-silicon-plan) |
| `MAC-MANAGED-001` | `SETTLED` | Apple Silicon Metal passed broad process-per-case and persistent managed matrices covering routes, shortcuts, toasts, synthetic checkout, close/back-to-app, one overlay target, zero managed timing, zero-FPS parking, isolation, and crash diagnostics. The current exact signed arm64 package passed its complete 55/55 route matrix at `/tmp/steam-bridge-macos-overlay-matrix-full-exact-final-20260719`, including screen-pixel visibility, input close, focus return, capture health, physical-host readiness, Steam injection, exact restored/maximized/minimized/simple-fullscreen host geometry, one-host reuse, and 120 Hz renderer pacing. All 1,130 native methods matched contract hash `25cfd24fac158d8768732933c153bab01aa1618ac44a6f39eeba23920a443ba4`. Steam-launched native Spaces fullscreen did not enter reliably, so the application smoke policy uses Electron simple fullscreen; Steam Bridge recognizes either state as fullscreen without owning the application transition. | Metal host/lifecycle/signing/launcher code, Electron/Steam client, supported route mapping, simple/native fullscreen detection, passive-notification completion, or the exact package changes. Use a focused changed-surface case or minimal/core suite before another full run. | [2026-07-19 Apple Silicon requalification](cross-platform-overlay-status.md#2026-07-19-apple-silicon-requalification) |
| `MAC-HOST-001` | `SETTLED` | Locked or display-asleep Macs must fail fast with native-host-unavailable state; they are not valid success-run environments. Steam health uses current connection and IPC evidence rather than isolated stale warnings. | Host-state detection or Steam health classification changes. Restart Steam only for current wedged IPC/multiple-target evidence. | [Latest macOS recovery evidence](cross-platform-overlay-status.md#latest-macos-recovery-evidence) |
| `MAC-QA-CONSOLE-LOCK-001` | `SETTLED` | An unchanged-candidate pass reached 23 green product cases before visual capture failed. The console had automatically locked immediately before the failures: TCC still allowed ScreenCaptureKit, but WindowServer rejected every screenshot. Fresh helper processes and restarting user `replayd` did not change that security boundary. The input helper now reads the authoritative IOKit system-console lock state and fails closed when unavailable; controller and profile preflights return `desktop_locked` before product launch or display mutation. A live locked-console probe produced that exact result in 2.3 seconds. The earlier SSH-scoped CoreGraphics session dictionary produced a stale false-positive after unlock and is a closed path; the false per-case ScreenCaptureKit-reset hypothesis and implementation were also removed. | Console-state detection, unattended assertion wrapper, ScreenCaptureKit/WindowServer locked-session behavior, or QA preflight ordering changes. Keep a continuous `UserIsActive` assertion for an authorized unattended run, but never induce lock, screen sleep, or system sleep as QA. | [unattended-console checkpoint](current-work.md#2026-07-31-macos-unattended-console-qa-finding), [input helper](../../scripts/macos-window-input.swift) |
| `MAC-QA-SWIFT-COMPILE-PACING-001` | `SETTLED` | Repeatedly interpreting the input helper compiled Swift for every exact-focus and snapshot action. On the 120 Hz macOS 26 profile, three otherwise healthy unchanged-candidate attempts then measured only 111.0-113.7 FPS Chromium presentation despite exact 120 FPS scheduling, stable focus/layer ownership, zero display-link skips, and zero crashes. Hiding or pausing other GUI apps was insufficient. The driver now accepts a precompiled input-helper executable; compiling the unchanged helper once produced two consecutive strict `fps-overlay` passes at 114.35-115.7 FPS across all phases, followed by a green live 120 -> 60 -> 120 overlay/display transition. The 95% gate was not changed. Removing the compiler delay also exposed one transient return-to-title helper failure: eight direct child-to-title repetitions proved exact pairing, and the runner now retries the same complete title proof for at most five seconds; an unchanged-candidate focused baseline then passed. | Input-helper launch semantics, Swift toolchain behavior, macOS/Chromium presentation tracing, or high-refresh QA orchestration changes. Compile the input helper once before release cadence work; do not weaken the pacing floor, weaken exact pointer/pair validation, or rerun unrelated green cases. | [high-refresh helper checkpoint](current-work.md#2026-07-31-macos-high-refresh-helper-finding), [actual-game runner](../../../fov4-steam/scripts/macos-actual-game-qa.mjs) |
| `MAC-OFFSCREEN-APPLICATION-HOST-001` | `CLOSED PATH` | A stable Electron 43.2 prototype put the game in a hidden shared-texture renderer and composited its IOSurface into one native AppKit/Metal application window. Steam and overlay rendering worked and the native presenter approached 120 FPS, but the actual game producer ran at 25.9 FPS at rest and 24.6 FPS under genuine movement, with 50-83 ms ordinary gaps and a 216.6 ms maximum despite Electron requesting 120. Focus emulation and three Chromium timing switches did not repair it. Restoring the visible Electron parent plus the existing attached Metal child immediately held 119.99-120.01 FPS at rest and during per-second movement samples. | Reopen only after a stable Electron release materially changes macOS offscreen begin-frame/shared-texture scheduling and an isolated control proves display-rate production. Keep one AppKit-attached child; never use a popup, companion, or application-host fallback on macOS. | [offscreen rejection checkpoint](current-work.md#2026-08-03-macos-offscreen-application-host-rejection-checkpoint), [Electron OSR implementation](https://github.com/electron/electron/blob/v43.2.0/shell/browser/osr/osr_render_widget_host_view.cc) |
| `MAC-OVERLAY-ENVIRONMENT-POLL-001` | `SETTLED` | Process sampling found regular overlay-environment polling calling `CGSessionCopyCurrentDictionary()` on Electron's UI thread; individual SkyLight queries blocked roughly 100-240 ms. An interim 250 ms utility-queue poll removed the UI-thread block, then exact movement comparison showed that even background WindowServer polling could perturb compositor cadence. Steam Bridge now initializes lock/display-sleep state once, updates it from public `NSWorkspace` sleep/wake/session notifications, and serves hot-path getters from atomics without a periodic poll. Exact signed/notarized/stapled actual-game QA then passed 130/130 cases, including five sustained-movement profiles with zero intervals over 50 ms. | Native environment detection, workspace notification ownership, lock/display-sleep semantics, or macOS overlay pumping changes. Never restore periodic WindowServer polling or move the blocking session dictionary query back to Electron's UI thread. | [offscreen rejection checkpoint](current-work.md#2026-08-03-macos-offscreen-application-host-rejection-checkpoint), [final movement checkpoint](current-work.md#2026-08-04-macos-high-refresh-qa-environment-isolation), [Metal child](../../crates/native/src/macos_metal_surface.mm) |
| `MAC-MOVEMENT-FIRST-AUDIO-001` | `SETTLED` | Exact 120 Hz movement samples isolated one 125-145 ms main-thread task at the first trusted gameplay key-down: timestamped task start was 565.8 ms and key-down was 566.0 ms, while key-up was clean. Pre-constructing Web Audio moved the same 133.2 ms cost before the sample. The configured client now recognizes the exact Steam cursor bridge as a native audio host and reuses constructor-time activation. After disposable causal proof, exact signed/notarized/stapled candidate `d6b9fcf478a8bf671954e006b420d3b5f9d14a7966360b32d545b96be18799d8` passed the complete 130/130 actual-game matrix. Its five movement cases ran at 119.867/59.867/48.000/59.867/59.733 FPS with zero intervals over 50 ms and zero Long Tasks. | Repeat only if audio host detection, Web Audio construction/preload, first gameplay input, movement instrumentation, or the exact client candidate changes. Do not repeat the causal unpatched A/B. | [movement/audio checkpoint](current-work.md#2026-08-03-macos-sustained-movementaudio-pacing-checkpoint), [final movement checkpoint](current-work.md#2026-08-04-macos-high-refresh-qa-environment-isolation), [configured client audio](../../../fov4-client-px-steam-commerce/src/engine/audio/PxAudioSystem.ts), [actual-game runner](../../../fov4-steam/scripts/macos-actual-game-qa.mjs) |
| `MAC-PARSEC-CAPTURE-PACING-001` | `SETTLED` | The unchanged patched actual game ran at 109-110/120 FPS while active Parsec capture consumed about 35% CPU and WindowServer about 45%. A 30-second renderer CPU profile was idle for 25.3 seconds; hot PX functions used only tens of milliseconds and long-animation frames had zero blocking script. Stopping only Parsec made the same running app pass at 119.735/120 FPS, p95 8.4 ms, maximum 27.5 ms, zero 50/100 ms stalls, zero Long Tasks, and stable input/focus/audio/canvas state. Parsec was restarted. Remote display capture is an invalid local cadence-qualification environment, not a product pacing fix target. | Remote-capture implementation, macOS WindowServer capture scheduling, or pacing environment changes. Disconnect Parsec/ARD/recorders before cadence proof; never weaken the 95% gate or cap the game to hide capture contention. | [movement/audio checkpoint](current-work.md#2026-08-03-macos-sustained-movementaudio-pacing-checkpoint), [macOS runner](../../../fov4-steam/scripts/macos-actual-game-qa.mjs) |
| `MAC-PASSIVE-120-HZ-001` | `CLOSED PATH` | Driving the full-size passive attached Metal child at the 120 Hz display rate doubled passive compositor work but did not repair renderer hitches: the focused local-addon run retained 158 ms and 333 ms renderer stalls, including one 333 ms renderer interval while Metal itself continued around 118/114 FPS. The experiment was reverted. Passive presentation remains an at-most-60-FPS Steam discovery heartbeat; active presentation follows the requested selected-display cadence. | Reopen only if Steam's passive surface-discovery contract materially changes and a new causal control proves the bounded heartbeat itself blocks Chromium. Never repeat the unchanged 120 Hz passive clock, detach/recreate the child, or use a popup/companion workaround. | [movement/audio checkpoint](current-work.md#2026-08-03-macos-sustained-movementaudio-pacing-checkpoint), [Metal child](../../crates/native/src/macos_metal_surface.mm) |
| `MAC-QA-OVERLAY-RESIZE-SYNC-001` | `SETTLED` | With the input helper precompiled, active-overlay title movement and the first shrink resize remained exact, but a separately synthesized expand drag began before the resized Electron/Metal pair had published fresh stable telemetry and produced no geometry delta. The failure reproduced in a focused run; the ordinary rapid non-overlay resize reversal stayed green. Synchronizing only between shrink and expand produced one green run, but the next independent run showed that shrink could likewise begin before the preceding title transactions settled. The active-overlay stress case now keeps visual capture uninterrupted and requires fresh healthy active-overlay child telemetry after the title pair and again after shrink, before each distinct resize transaction. Later focused runs passed both exact resize deltas, frame return, full overlay coverage, later maximize/minimize/focus/fullscreen transitions, cleanup, display restoration, and zero crashes. | Input-helper execution pacing, overlay-active resize behavior, child telemetry synchronization, or the overlay-state stress sequence changes or regresses. Do not reintroduce source-compilation delay, weaken geometry/pairing/visual gates, or treat separate mouse transactions as one continuous reversal. | [high-refresh helper checkpoint](current-work.md#2026-07-31-macos-high-refresh-helper-finding), [actual-game runner](../../../fov4-steam/scripts/macos-actual-game-qa.mjs) |
| `MAC-ACTIVE-FULLSCREEN-ALPHA-001` | `SETTLED` | An active attached Metal child cleared requested-opaque black across the entire parent after simple fullscreen removed the titlebar. Steam's translucent pixels then composited against black rather than the Chromium game: the old candidate's settled frame was 86.5-86.7% black with mean luma about 0.020. The macOS child now keeps requested opacity only for a titled parent whose titlebar consumes frame; native/application full-frame states retain the same attached child but use transparent Metal clear, and titled-window restore re-applies the existing opacity/corner policy. A new active-fullscreen visual gate rejects the exact old signature and requires retained game luma/structure with bounded black growth. Signed/notarized/stapled stable-43.2.0 focused receipts `focused-fixed-overlay-state-20` and `-21` passed the full active state sequence at 120 Hz with fullscreen luma 0.084, black 0.294/0.293, exact settled boundaries, same-child/drawable alignment, clean restore, and zero crashes. Predicate counters also proved the transition's old signature-only flags came from the intentional opaque-to-alpha switch/UI reflow: baseline-reversion and all broad coverage-loss counts were zero. The exact repaired candidate subsequently passed `final-06`: 125/125 executions and 37/37 canonical requirements across native Retina 120/60/48 Hz, low-Retina 60 Hz, and scale-1 60 Hz, with the active-fullscreen case green in every profile, no cadence exception, zero crashes, and exact display restoration. | macOS opacity policy, parent titlebar/fullscreen detection, Metal clear/layer opacity, active-fullscreen visual comparison, or fullscreen transition classification changes or regresses. Keep one attached child. Never restore always-opaque full-frame behavior or use a popup/companion as a fallback. | [active-overlay fullscreen checkpoint](current-work.md#2026-07-31-macos-active-overlay-fullscreen-alpha-finding), [Metal child](../../crates/native/src/macos_metal_surface.mm), [visual helper](../../scripts/macos-window-visual-qa.swift) |
| `MAC-AUTOMATION-001` | `SETTLED` | SSH-launched `screencapture` and `osascript` are attributed to `/usr/libexec/sshd-keygen-wrapper`. Early runs correctly stopped because that host lacked Screen & System Audio Recording and Accessibility authority. After those permissions were granted and SSH reconnected, the exact signed full matrix passed 55/55 with screen-pixel capture and Escape-close evidence. The helper still fails closed to an exact-PID `gameoverlayui` plus a visible Steam Helper window overlapping at least 25% of the exact presenter host only when pixel capture itself is unavailable; fixtures prove PID, overlap, source, and rectangle handling. | Repeat after the automation host identity, macOS TCC grants, pixel-capture/input helper, exact-PID/overlap fallback, or proof schema changes. Do not install another click utility or weaken the fallback. | [2026-07-19 Apple Silicon requalification](cross-platform-overlay-status.md#2026-07-19-apple-silicon-requalification) |
| `MAC-ATTACHED-CHILD-001` | `SETTLED` | The macOS product architecture is one Electron parent plus one Metal `NSWindow` attached through AppKit's parent/child relationship. Popup, companion, independently managed top-level, and recreate-on-transition variants are closed paths: they previously caused whole-window coverage, tiny/partial surfaces, lost corners, focus/toolbar conflicts, flicker, hangs, and crashes. A missing weak parent fails closed; advisory `occlusionState` is not a presentation gate because attached-child/fullscreen coverage can make it self-referential. | Reopen only after a material Steam hook-selection or renderer-architecture change with exact live pixel, geometry, focus, lifecycle, and crash proof. Never use a popup fallback for an attached-child failure. | [macOS focused closure](current-work.md#2026-07-26-macos-focused-closure-checkpoint) |
| `MAC-ELECTRON-RENDERER-FOCUS-001` | `SETTLED` | Restoring only the BrowserWindow can leave Chromium's document unfocused on macOS. Steam Bridge now restores both the window and `webContents`; FOV4 defers its application-owned simple-fullscreen focus until native menu dispatch returns. Focused receipts 28 and 31 proved renderer focus after overlay recovery and both inside and after simple fullscreen, with the same attached child and zero crashes. | Electron focus semantics, generic restore-focus policy, FOV4's simple-fullscreen/menu policy, or renderer-focus evidence changes, or a focused focus/fullscreen case regresses. | [macOS focused closure](current-work.md#2026-07-26-macos-focused-closure-checkpoint) |
| `MAC-SIMPLE-FULLSCREEN-LAYOUT-001` | `SETTLED` | AppKit can publish its final `contentLayoutRect` one run-loop turn after Electron's terminal `resized` event. Receipt 30 retained the transient titlebar-inclusive `1280x752` child after simple-fullscreen exit. Steam Bridge now performs one Darwin-only, coalesced, cancellable next-turn geometry reconciliation; receipt 31 proved exact `1280x720` child/content alignment, rounded-corner policy, exact restoration, and zero crashes. | macOS Electron resize events, AppKit content-layout timing, child geometry synchronization, simple fullscreen, or close/cancellation behavior changes, or the focused fullscreen/layout case regresses. | [macOS focused closure](current-work.md#2026-07-26-macos-focused-closure-checkpoint) |
| `MAC-CHECKOUT-FULLSCREEN-PANEL-001` | `SETTLED` | A configured-product checkout probe first resized the parent to `1100x662` with an exact `1100x630` child, then entered application-owned simple fullscreen while Steam remained active. CoreGraphics and native diagnostics agreed on one `1728x1117` parent/child pair, a `1728x1117` Metal view, `3456x2234` drawable at scale 2, exact parent-content matching, roughly 118-120 FPS presentation against 120 Hz, and zero drawable/render failures. Pixel inspection showed the dimmed Steam surface covering the complete game area while Steam retained its own fully contained floating checkout-browser panel. That internal panel is Steam-owned UI, not an undersized native host and not the historical tiny/partial surface failure. | Repeat only if child/view/drawable geometry no longer matches the parent, Steam's internal panel escapes the child or becomes unusably small, the relevant Steam/AppKit/Metal sizing path changes, or the configured-product fullscreen case visibly regresses. Never detach, recreate, promote, or replace the child merely to stretch Steam-owned browser chrome. | [configured-product commerce checkpoint](current-work.md#2026-07-30-configured-product-macos-commerce-checkpoint) |
| `MAC-CONFIGURED-CHECKOUT-CORRELATION-001` | `SETTLED` | A real configured-product subscription checkout remained active for more than eleven minutes, crossing both the client's five-minute fail-closed boundary and the consumer shell's former ten-minute active timeout. Two native Escape actions each produced Steam's provisional inactive/active Cancel Transaction sequence against the same checkout; correlation, child identity, geometry, and crash health remained exact. The consumer shell now bounds only prepared intent and retains correlation for as long as Steam reports that checkout active. The client accepts a late authorization result only for the exact known order. This state ownership is in the consumer shell/client, not Steam Bridge native presentation. | Consumer checkout correlation, client authorization state, Steam's Cancel Transaction callback order, renderer teardown, or terminal close semantics change, or the live sequence regresses. Never add an elapsed timeout to an actively reported checkout and never replace the attached child. | [configured-product commerce checkpoint](current-work.md#2026-07-30-configured-product-macos-commerce-checkpoint) |
| `MAC-CONFIGURED-INVENTORY-PERSISTENCE-001` | `SETTLED` | The configured-product non-spending lane moved the trusted Steam inventory from eleven rows/twelve units to exactly zero, one accepted transfer at a time. Rapid double-clicking transferred one unit, a full mailbox produced a clean no-op, and transferred items appeared in mail without an extra send. Direct Steam API and rendered Gem Shop state remained empty after Refresh, shop close/reopen, real logout/reload/login, and exact process close plus Steam-owned relaunch. The relaunched actual game restored the DPR-matched surface and created no new crash dump. | Steam inventory API, mail capacity/reservation, transfer acknowledgment, logout/recovery, app relaunch, or configured-product integration changes, or any persistence edge regresses. Do not spend money and do not expose item, account, order, or transaction identifiers in retained evidence. | [configured-product commerce checkpoint](current-work.md#2026-07-30-configured-product-macos-commerce-checkpoint) |
| `MAC-QA-KNOWN-CADENCE-RECOVERY-001` | `SETTLED` | A focused same-resolution fixed-60 run proved the accepted Chromium factor-two defect can poison the immediately following baseline if the harness merely restores window state. The runner now recognizes the exact Electron 43.2.0/focus/native-Metal/factor-two signature independently from final-only authorization, performs one clean Steam-owned warm relaunch, and requires a fresh selected-rate scheduler sample before later cases. Live attempt 03 retained factors `[1,1,1,2]`, 107 skipped callbacks, exact 30 FPS post-restore, a new app process, 60.000 FPS after recovery, and a green full traced baseline. Passive Metal validation follows the production policy: an inactive child is capped at 60 FPS on higher-refresh displays, while alignment, progress, driver, failure counters, and configured frame-rate policy remain strict. | Stable Electron contains the upstream repair, the exception signature/policy changes, warm relaunch or cadence sampling changes, or the exact transition/baseline sequence regresses. Never authorize the exception outside final mode, accept an incomplete signature, lower the 95% renderer gate, or continue after failed recovery. | [configured-product commerce checkpoint](current-work.md#2026-07-30-configured-product-macos-commerce-checkpoint), [Chromium M152 cached-interval change](https://chromium.googlesource.com/chromium/src/+/b43494c23fc0af79df367767396e3e216bd91e97) |
| `MAC-CONFIGURED-BROAD-QA-001` | `SETTLED` | The latest configured-product candidate containing the then-current client-px main completed the safe 25-case actual-game matrix under five exact display profiles: same-resolution 120/60/48 Hz, low-Retina 60 Hz, and scale-1 60 Hz. All 125/125 executions and 37/37 canonical requirements passed with no exception used. The isolated pacing transition precedes its independent baseline and the broad live transition is terminal, so cadence residue cannot taint later cases. All 125 renderer/presentation samples met the 95%-108% rate gate, all 275 attached-Metal samples passed strict policy/geometry/health checks, all 395 visual records passed, and all crash categories were zero. Three of 4,585 active-overlay transition frames carried a one-frame outer-right diagnostic from fixed-output ScreenCaptureKit resampling, but zero frames failed coarse coverage or dropout and all six native-scale exact overlay captures per profile passed right/bottom edge proof. Cleanup closed the exact app, preserved Steam, removed the private launcher environment, and restored mode 54 at 1728x1117 logical / 3456x2234 backing / 120 Hz. | Candidate identity, client-px main, Steam Bridge/macOS native presenter, shell window/input/fullscreen/display policy, Electron/Chromium, Steam client, QA contract, or one covered behavior materially changes or regresses. Retest only affected cases until individually green; rerun the broad matrix once for a new release candidate. Never add lock/sleep cases or authorize spending. | [configured-product commerce checkpoint](current-work.md#2026-07-30-configured-product-macos-commerce-checkpoint) |
| `MAC-HIDDEN-METAL-PRESENT-001` | `SETTLED` | Receipt 32 proved the hidden/minimized attached MTKView incorrectly kept drawing at display cadence and accumulated hundreds of unpresented drawables. The existing child now pauses when it or its parent is not visible, the parent is miniaturized, or `NSApp` is hidden, and resumes without recreation. Receipt 34 proved passive minimize and application-hide quiescence; receipt 36 proved active minimize with the same child, `viewPaused=true`, stable draw/present/drawable-failure/render-failure counters, exact restore, 120 Hz continuation, and zero app/overlay/Steam/graphics crashes. Do not gate this policy on advisory occlusion state. | Native Metal visibility/pause policy, parent/child attachment, minimize/hide/show/restore, MTKView scheduling, diagnostics, or Electron/Steam/AppKit behavior changes, or a focused visibility case regresses. | [macOS focused closure](current-work.md#2026-07-26-macos-focused-closure-checkpoint) |
| `MAC-QA-ACTIVATION-EVIDENCE-001` | `SETTLED` | Steam's activation callback is authoritative but does not promise overlay persistence across prolonged minimize. Receipt 34 showed a clean inactive callback after restore; the harness now proves fresh active same-child telemetry and checkpoints callbacks before minimize, requires an adjacent hidden pair with one resolved boolean state and the established surface identity, and rejects samples spanning visibility/state/phase boundaries. Retention requires no exact inactive callback plus active hidden and restored telemetry. Deactivation requires an exact inactive callback and a later exact active callback in order; a passive restore must prove passive input/alignment and reopen only through the public Friends route. Receipt 35 exposed a verifier-only false negative because the log reader ignored the exact callback marker. The reader now retains it, rejects stale/wrong-app/telemetry-only evidence, and requires healthy active same-child telemetry and visual proof. Receipt 36 passed the full affected stress path with `deactivated-reactivated` recorded; post-receipt adversarial unit tests lock every lifecycle branch. | Steam overlay callback schema/lifecycle, QA menu routing, log-reader markers, receipt sanitization, or minimize classification changes. Never replace the callback with telemetry-only or window-event evidence. | [macOS focused closure](current-work.md#2026-07-26-macos-focused-closure-checkpoint) |
| `MAC-ELECTRON-REFRESH-001` | `SETTLED` | Receipts 59, 61, and 64 respectively synchronized, repainted, and fully paused the same attached child while Electron's 120 -> 60 -> 120 restore still degraded, exonerating the child. Plain Electron 43 receipt 68 reproduced roughly 120 -> 60 -> 90 with no Steam, Bridge, child, or game. Receipt 69 traced the same persistent `ExternalBeginFrameSourceMac` callback at 120.003 -> 59.997 -> 94.325 FPS, missing roughly every fourth restored callback. Electron 44/Chromium 152 receipt 70 and CoreGraphics session-scope receipt 71 reproduced it. Browser-only `CADisplayLink` receipts 73 and 75 restored scheduler and external begin-frame cadence to 120 -> 60 -> 120 both exactly focused and fully occluded/unfocused, proving backgrounding was not causal. Their light-CSS physical-presentation score is deliberately non-authoritative and does not weaken the actual-game gate. Receipt 72 failed its analysis import before a display change and is invalid infrastructure evidence. | Repeat the minimal causal matrix only if Chromium's Mac display-link implementation, the feature arm, Electron major version, or trace event semantics materially change. Do not rerun it merely because actual-game qualification is pending. | [refresh-transition checkpoint](current-work.md#2026-07-26-macos-refresh-transition-causal-checkpoint), [receipt history](macos-overlay-notes.md#2026-07-26-refresh-rate-transition-causal-isolation) |
| `MAC-ELECTRON-REFRESH-WORKAROUNDS-001` | `CLOSED PATH` | Native-child retiming, forced child pause, Electron repaint, Electron 44 alpha, and CoreGraphics transaction-scope changes do not repair Chromium's persistent default `CVDisplayLink`. The GPU-then-browser `CADisplayLink` arm is also rejected: Chromium abandoned it after random hangs following power resume and a sleep/wake unresponsive-UI regression. Keep the one attached child. An offscreen Electron renderer feeding an IOSurface-backed child is contingency architecture only, not a fallback to implement alongside the selected path. | Reopen an individual path only if its owning upstream behavior materially changes and a focused isolated control provides new causal evidence. Reconsider OSR/IOSurface only if the browser-only arm fails required actual-game or recovery QA; never convert the child to a popup/companion. | [refresh-transition checkpoint](current-work.md#2026-07-26-macos-refresh-transition-causal-checkpoint), [Chromium GPU-to-browser revert](https://chromium.googlesource.com/chromium/src/gpu/+/147518cb6e20bc4a00d641ef96e09c0bac3424ff) |
| `MAC-DISPLAY-SUPERVISOR-NESTING-001` | `SETTLED` | Final RC80 attempt 01 passed the complete 120 Hz profile, then failed the 60 Hz `display-live-transition` because the harness nested two application-scoped CoreGraphics owners. The inner owner observed its explicit 60 Hz restore, but its process exit exposed the session's 120 Hz mode instead of reviving the outer process's application scope. A single persistent profile owner with atomic sequence/token mode requests replaced that invalid topology. Focused receipt `fov4-macos-qa-rc80-display-control-60-01` then proved 60 -> 120 -> 60 both passively and with Steam active, exact same-child alignment, target-rate presentation, final 120 Hz desktop restoration, Steam survival, and zero crashes on unchanged RC80. Focused receipt `fov4-macos-qa-rc80-display-control-pacing-60-01` separately proved the only other affected caller with exact 60 FPS renderer and Chromium restore cadence. The nested helper/hold-file code is physically removed and statically rejected. | Repeat only if the display supervisor ownership/control protocol, CoreGraphics transaction lifetime, temporary display-transition implementation, or macOS display-configuration semantics change, or a focused transition regresses. Never reintroduce nested application-scoped helpers or use session-scoped persistent mutation as a fallback. | [RC80 checkpoint](current-work.md#2026-07-27-rc78rc79rc80-recovery-candidate-checkpoint) |
| `MAC-QA-GESTURE-CLAMP-001` | `SETTLED` | Final RC80 attempt 02 passed 75/75 executions across the complete 120, 60, and 48 Hz profiles, then low-Retina mode 7 exposed a harness boundary error. A 900x650 staging frame at y=60 left only 20-41 points of downward work-area travel on the 1168x730 desktop, so AppKit correctly clamped requested 60/70-point moves. Focused unchanged-RC80 receipt `fov4-macos-qa-rc80-low-retina-gesture-01` proved the move repair at an interim 900x540. Final attempt 03 then showed that 540 could shrink only 28 of the requested 60 points above the app's 512-point outer-frame minimum. The mathematically valid shared staging frame is 900x600. The next focused sweep emitted green results for every shared move/resize caller and exposed a mislabeled 1128x520 aspect endpoint whose 1128x488 game area is narrower than 21:9. The wide endpoint now uses the 512-point minimum outer height, yielding a genuine 1128x480 wide game area; focused receipt `fov4-macos-qa-rc80-aspect-resize-reversal-01` passed reversal and both aspect branches. Zoom then passed, while overlay stress exposed only a three-decimal corner-opacity boundary: score 0.1 and edge 0.998 were rounded but rejected by strict `<0.998`. The contract now accepts `<=0.998` while rejecting 0.999/1.000; focused receipt `fov4-macos-qa-rc80-overlay-rounding-01` passed the complete active state stress. | Repeat only if gesture staging, app minimum/chrome geometry, aspect endpoint selection, visual-helper precision, rounded-corner thresholds, or an affected case changes or regresses. Do not weaken exact delta assertions to accept clamping or accept 0.999/1.000 as rounded. | [RC80 checkpoint](current-work.md#2026-07-27-rc78rc79rc80-recovery-candidate-checkpoint) |
| `MAC-CHROMIUM-POST-RESTORE-HALF-RATE-001` | `SETTLED` | Clean final RC80 attempt 04 passed the complete 120 Hz profile and the 60 Hz baseline/menu/input/live-transition cases, then restored from the isolated 60 -> 120 -> 60 pacing transition with renderer rAF, PID-pinned Chromium presentation feedback, and the following independent baseline all fixed at exactly 30 FPS. Focus and production canvas identity remained exact. The same attached Metal child stayed paired, aligned, healthy, configured for 60 Hz, and presented at 60 FPS with no drawable or device errors. Cleanup restored exact desktop mode 54/120 Hz and Steam survived. Focused prefix reduction proved `warm-relaunch` is the only required predecessor. Causal receipt `fov4-macos-qa-rc80-half-rate-causal-02` recorded 1,287 browser display-link callbacks, 106 skipped-vsync events, and preferred factors `[1,1,1,2]`; post-restore and the next baseline skipped essentially every other callback, with no new interval-decider result during the failing transition. Temporary diagnostic receipt `fov4-macos-qa-rc80-half-rate-input-nudge-03` used one non-clicking CDP mouse move to force two fresh interval decisions, six factor-one preferences, zero skipped callbacks, and exact 60 FPS through both probes. Signed/notarized RC81 proved a zero-distance Electron move was insufficient; RC85 proved an immediate one-DIP move-and-restore pair was insufficient. Exact signed/notarized/stapled RC86 proved the remaining single one-DIP move was also insufficient: factors `[1,1,1,1,1,2,1,2]`, 102 skipped callbacks during the transition, 91/91 skipped after restore, and exact 30 FPS through the following baseline while Metal remained healthy at 60. Synthetic renderer input is a closed product path. RC87 disabled `UseFrameIntervalDecider`; attempt 01 passed, but identical attempt 02 failed at exact 30 FPS with factors `[1,1,1,2]`, 103 transition skips, and 90/91 post-restore skips even though live renderer arguments proved the switch was present. Chromium 150.0.7871.129 has made the decider unconditional and no longer declares that feature, making attempt 01 a false green and the switch a closed path. RC88 disabled `SingleVideoFrameRateThrottling`; its exact signed/notarized/stapled focused receipt failed at exact 30 FPS through the transition and following baseline with 1,286 callbacks, 108 skips, and factors `[1,1,1,2]`. Cleanup restored mode 54/120 Hz, Steam survived, and crash checks were zero. The matcher override is also a closed path and is removed. Chromium 150's external macOS begin-frame source stores and applies the factor on every callback and recreates on a same-display notification only if the display link reports itself invalid; this link remained valid. Its zero-preference hardware query can run ahead of the callback-updated cached minimum and calculate factor two. M152 commit `b43494c23fc0af79df367767396e3e216bd91e97` removes that ahead-of-callback query from the browser-display-link path and uses the last known cached minimum. Exact signed/notarized/stapled RC89 fingerprint `cb1d53b7631ba74444b0d06eaac6d905351e5be91acdbb5894620d4b3a4c5b98` tested that change under Electron 44.0.0-alpha.7 / Chromium 152.0.7969.0. Two identical reduced-prefix receipts passed with factors `[1,1]`, zero skips, approximately 60 FPS after restore and through the independent baseline, exact display restoration, Steam survival, and zero crashes. The product owner accepted only this exact evidence-bound defect signature on stable Electron 43.2.0 for the current release; it remains visible in schema-v2 receipts and does not weaken any other gate. | Repeat only to remove the exception after an exact stable Electron containing the M152 repair or a supported backport exists, or if the observed signature changes. Preserve the strict 95%-108% fixed-rate gate for every non-excepted case. Never ship the isolation alpha or revive feature flags, synthetic input, popup/companion architecture, or child retiming/recreation as workarounds. | [RC80/RC81 checkpoint](current-work.md#2026-07-27-rc78rc79rc80-recovery-candidate-checkpoint), [RC85/RC86 checkpoint](current-work.md#2026-07-28-rc85-actual-game-checkpoint), [Chromium 150 decider wiring](https://chromium.googlesource.com/chromium/src/+/refs/tags/150.0.7871.129/components/viz/service/frame_sinks/root_compositor_frame_sink_impl.cc), [Chromium 150 subsampling](https://chromium.googlesource.com/chromium/src/+/refs/tags/150.0.7871.129/components/viz/service/frame_sinks/external_begin_frame_source_mac.cc), [Chromium M152 cached-interval change](https://chromium.googlesource.com/chromium/src/+/b43494c23fc0af79df367767396e3e216bd91e97) |
| `MAC-BROWSER-CADISPLAYLINK-RC-001` | `OPEN` | Steam Bridge exposes the exact Chromium `EnabledInBrowser` arm as an explicit startup option that defaults off, is eligible only on Darwin/macOS 14+/Chromium 150+, and locks conflicting decisions before readiness. FOV4 explicitly opts in on Darwin; the Steam shortcut carries no raw feature switches. Exact signed/notarized/stapled RC77 fingerprint `74566f3c33cbe87d85a8069a5c65cc212a1b5f1c03ca9b56a3ab2ba5695f9bd4` passed warm relaunch, actual-game 120 -> 60 -> 120 recovery, overlay pacing/lifecycle, and native-Spaces transition diagnostics. GPU-child termination exposed PX's correct terminal WebGL context-loss behavior, now owned by a cooldown-bounded one-shot FOV4 shell reload, bounded route restore, and fresh-auth recovery. RC78 was rejected before launch because reload could not restore the route; RC79 was rejected before bind because invalid/incomplete routes could retain stale auto-resume intent. Exact RC80 fingerprint `13a2e73695b656c9ea1b0f1cb1595bf5de9af20411ac2fae38eb2c9756fcfb71` added fail-closed route clearing and passed its source, signing, transactional bind, and focused automatic GPU-recovery gates. Exact later candidate `d6b9fcf478a8bf671954e006b420d3b5f9d14a7966360b32d545b96be18799d8` passed the complete 26-case/five-profile actual-game suite (130/130), including baseline/overlay/post-close presentation cadence, live display changes, warm relaunch, GPU recovery, every attached-child window transition, exact display restoration, and zero crashes. Display sleep on RC77 recovered the same app/child at about 120 Hz but exposed unpresented drawing while the target display slept, now fixed by pausing the existing MTKView from the CoreGraphics asleep state. Lock-capable sleep execution is retired by the separate closed-path row below. | The actual-game condition is satisfied. Close only after the current exact app passes the separate final 55-route gate. Do not revive child retiming, fixed child-click lifecycle, popup/companion architecture, PX context reuse, the GPU-then-browser arm, or lock-capable sleep QA. Never bind, launch/focus, lock, or change display state while an operator is using the Mac. | [RC77 checkpoint](current-work.md#2026-07-27-rc77-actual-game-checkpoint), [RC80 checkpoint](current-work.md#2026-07-27-rc78rc79rc80-recovery-candidate-checkpoint), [final actual-game checkpoint](current-work.md#2026-08-04-macos-high-refresh-qa-environment-isolation), [receipt history](macos-overlay-notes.md#2026-07-27-exact-rc77-actual-game-qualification), [Chromium field-trial arms](https://chromium.googlesource.com/chromium/src/testing/+/8d0688b4e473d74459011a72d3d4fd7aac2967cc), [Chromium macOS gate](https://github.com/chromium/chromium/blob/150.0.7871.129/ui/display/mac/display_link_mac.mm#L27-L79) |
| `MAC-RC80-GPU-RECOVERY-001` | `SETTLED` | This row supersedes the earlier RC80 "unbound/unlaunched" checkpoint. Transactional bind receipt `shortcut-binding-03` is green for exact candidate `13a2e73695b656c9ea1b0f1cb1595bf5de9af20411ac2fae38eb2c9756fcfb71`. Focused receipt `/private/tmp/fov4-macos-qa-rc80-gpu-recovery-01` proves the same app and attached child survived GPU-process replacement, automatic shell reload returned to the production game without driving Play, renderer cadence remained 120 Hz, Chromium presentation feedback was 119.003 Hz, all 2306 transition frames avoided dropout/cover/flash failure, cleanup and display restoration passed, and all crash categories were zero. | Repeat only if GPU recovery, bounded route restoration, fresh-ticket reacquisition, Chromium presentation feedback, attached-child recovery, or the exact candidate changes. | [RC80 checkpoint](current-work.md#2026-07-27-rc78rc79rc80-recovery-candidate-checkpoint) |
| `MAC-LOCK-CAPABLE-SLEEP-QA-001` | `CLOSED PATH` | RC80 receipt `/private/tmp/fov4-macos-qa-rc80-display-sleep-01` failed with `window_state_mismatch` and no accepted sleep/wake/restore evidence, while cleanup, display restoration, candidate re-fingerprint, Steam survival, and all crash categories remained green. More importantly, the attempt invoked macOS's security lock screen and required a manual operator unlock. Full-system sleep was not attempted. Both sleep case implementations, their CLI acknowledgement, and their release-matrix entries are removed; the former external controller is a refusal-only tombstone. Historical sanitized receipts remain readable for audit. | Never repeat as QA or a release gate. Reconsider only after an explicit product requirement and a design that cannot lock, sleep, or otherwise take control of the operator's computer. | [RC80 checkpoint](current-work.md#2026-07-27-rc78rc79rc80-recovery-candidate-checkpoint) |
| `MAC-RC80-ROUTE-GATE-001` | `OPEN` | Static expansion proves the safe `full` route suite is exactly 55 unique cases and contains no unavailable, locked, display-asleep, or sleep action. Exact package `f84f37f329be767c6576aa86d18bfa858287be7faa765c4839265e06946add5c` produced a signed smoke app with 1141/1141 native methods and tree fingerprint `d8511aa790db74f5b5b2835892c2534ff920bf7695373efb1d5d35d9f29c35f9`. Owner-only controller `1cfbc9252ec6a615bed78c017f9f3eb5e784cca458ee12f098be8684ce83b627` pins every route input, fingerprints before/after, accepts only the full suite, and refusal-tests without creating output. The exact-tarball packager path and complete 347-test/package-smoke gates are green. | Close only after the current exact app passes the live 55/55 full route matrix following the final actual-game receipt. Never substitute the unavailable suite or repack an unpinned checkout. | [RC85 checkpoint](current-work.md#2026-07-28-rc85-actual-game-checkpoint) |
| `MAC-QA-LIVE-DISPLAY-STAGING-001` | `SETTLED` | A scale-1 focused RC85 live-transition run entered the 1168x730 low-Retina mode but reused the pre-transition baseline origin, staging a 900x600 frame at `(360,270)` with its corner at `(1260,870)` outside the live desktop. Title dragging remained reachable while bottom-right resize correctly produced no delta. Exact-edge, one-point-inset, and a diagnostic -8..8 offset sweep all failed identically with healthy pointer cleanup and exact child/parent pairing, ruling out an AppKit resize-band or Steam-child defect. FOV4 commit `694636d` clamps the staging origin against the temporary mode while reserving the full gesture travel. Unchanged exact RC85 focused receipt `fov4-macos-qa-rc85-scale1-display-live-clamp-01` then passed physical title/corner gestures plus passive and active Steam scale transitions, alignment, visual health, pacing, cleanup, display restoration, Steam survival, and zero crashes. | Repeat only if temporary display-mode selection, gesture staging/travel requirements, macOS coordinate semantics, or the display-live case changes or regresses. Do not add a native resize workaround, retarget/recreate the child, or retry edge offsets for this settled off-screen target error. | [RC85 checkpoint](current-work.md#2026-07-28-rc85-actual-game-checkpoint) |
| `MAC-QA-ZOOM-VISUAL-001` | `SETTLED` | RC82/RC85 scale-1 overlay stress appeared to lose rounded corners after gesture staging. Bounded in-session capture-source evidence proved the same parent changed from `1280x752` to the exact `1920x1170` work-area frame only after the harness intentionally invoked AppKit zoom; app telemetry independently recorded the animated transition, `maximized=true`, exact attached-child alignment, and exact restore. The state-specific contract accepts either a coherent rounded inset standard frame or a coherent square work-area boundary, rejects mixed seam/corner states, and keeps restored windows strictly rounded. Focused unchanged-RC85 scale-1 evidence first passed the square shape. The authoritative configured-product broad receipt subsequently passed `overlay-state-stress` in all five profiles, including max-Hz 2x Retina and scale 1, with exact restore, strict native-scale edge/corner captures, same-child geometry, Steam survival, zero crashes, and final desktop restoration. Its three one-frame outer-right transition diagnostics were fixed-output ScreenCaptureKit resampling only: coarse coverage/dropout stayed green and every exact native capture passed. The helper exposes only bounded geometry/scale metrics, and the pinned controller hashes the exact fixed-name contract imported by the driver. | Repeat only if AppKit zoom behavior, display profiles, corner/edge contract, capture scaling, helper precision, attached-child geometry, or an affected case changes or regresses. Never weaken restored-window rounded-corner proof or substitute a different verified file for the executed contract. | [configured-product commerce checkpoint](current-work.md#2026-07-30-configured-product-macos-commerce-checkpoint) |

| `MAC-MOVEMENT-PACING-002` | `SETTLED` | The first-key stall was client-px Web Audio initialization and is fixed live by `d7b61b51`. A separate intermittent compositor hitch correlated with Steam Bridge querying CoreGraphics display/session state from the MTKView draw path and a 250 ms poller. The bridge now performs one initial read and tracks public `NSWorkspace` sleep/wake/session notifications in atomics; it retains the one AppKit-attached child and removes no presentation functionality. Three source-linked 120 Hz movement runs passed before the signed candidate sweep. The remaining intermittent 60 Hz red was proven to be the combination of two QA-only observers; FOV4 now skips the 100 ms full overlay snapshot while the Steam surface is inactive, without changing active-overlay diagnostics. Two ordinary-instrumentation 60 Hz proofs then passed at 59.867 and 59.933 FPS. Exact signed/notarized/stapled candidate `d6b9fcf478a8bf671954e006b420d3b5f9d14a7966360b32d545b96be18799d8` passed final receipt `/private/tmp/fov4-macos-qa-final-130-20260804-04`: 130/130 executions across five profiles, movement at 119.867/59.867/48.000/59.867/59.733 FPS, zero intervals over 50 ms, zero long tasks, zero crashes, complete hashed receipt, and exact 120 Hz display restoration. | Repeat focused pacing only if audio host detection, display/session lifecycle monitoring, the Metal draw path, native telemetry sampling, movement observer configuration, or movement receipt shape changes. Never restore periodic WindowServer polling or revive the application-host/popup/companion path. | [movement-pacing checkpoint](current-work.md#2026-08-04-macos-movement-pacing-causal-fix), [environment/final receipt checkpoint](current-work.md#2026-08-04-macos-high-refresh-qa-environment-isolation) |

## Cross-platform Invariants

| ID | Status | Finding | Repeat only when | Evidence |
| --- | --- | --- | --- | --- |
| `CROSS-PURCHASE-001` | `SETTLED` | App ID `480` proves generic overlay plumbing and synthetic routing only. It cannot prove real purchase content or authorization. | Never. Real proof always uses private runtime inputs with only sanitized committed evidence. | [Purchase overlay checklist](cross-platform-overlay-status.md#purchase-overlay-checklist) |
| `CROSS-MATCHMAKING-SHUTDOWN-001` | `SETTLED` | A live App ID `480` probe proved that invalidating waiter generations alone was insufficient: shutting down with an unregistered one-shot matchmaking server-list request emitted a Steam matchmaking lock failure and terminated the Node process with an access violation. The replacement registers list and ping/player/rules queries while holding the shared lifecycle lock, gives shutdown sole cancellation/release ownership, and keeps callback response state alive until its future rejects. The rebuilt native addon rejected the pending list in 32 ms, rejected a simultaneous open request as stale after reinitialization in 69 ms, and rejected a generic pending leaderboard call in 15 ms, with clean exits. A following stress probe passed 20 generic API and 10 matchmaking init/request/immediate-shutdown cycles with prompt rejections and zero crashes. | Repeat only if matchmaking request registration, active-query ownership, client shutdown ordering, lifecycle generations, manual dispatch, or Steam SDK behavior changes, or a shutdown-with-pending-work crash regresses. Do not test lifecycle safety by leaving a native request unregistered. | [0.3.12 checkpoint](current-work.md#2026-08-01-0312-security-and-lifecycle-release-checkpoint) |
