# steam-bridge

Native-backed Steamworks bridge for Electron and Node.

This package exposes the FOV4 V1 Steam API and a compatibility adapter for the
FOV4-used `steamworks.js` shape. See the repository README for build and release
details.

